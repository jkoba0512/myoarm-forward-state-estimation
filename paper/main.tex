% myoArm Forward State Estimation -- TNNLS draft
%
% Build:
%   cd paper && pdflatex main && bibtex main && pdflatex main && pdflatex main
%
% TNNLS uses the standard IEEEtran.cls (journal mode). Authors / affiliations
% are intentionally left blank for the v0 draft.

\documentclass[journal]{IEEEtran}

\usepackage{cite}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{algorithmic}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{textcomp}
\usepackage{xcolor}
\usepackage{url}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,calc,fit,backgrounds}
\usepackage{hyperref}
\hypersetup{colorlinks=true,linkcolor=blue,citecolor=blue,urlcolor=blue}

\graphicspath{{../figures/}}

% (No project-specific macros are needed in the final draft; the \K
% shortcut and a few unused helpers were removed during the notation
% review. K, K*_ol, K*_cl, H, h are inlined as plain math.)

\begin{document}

\title{How a Predictive State Observer Can Self-Adapt Its Sensory
Prediction-Error Correction Gain\\
Closed-Loop Evidence from a Muscle-Driven Reaching Task}

\author{Jun~Kobayashi%
    \thanks{Manuscript received [date]; revised [date]. J.~Kobayashi
    is with the Department of Intelligent and Control Systems,
    Graduate School of Computer Science and Systems Engineering,
    Kyushu Institute of Technology, 680-4 Kawazu, Iizuka, Fukuoka
    820-8502, Japan (e-mail: kobayashi.jun184@m.kyutech.ac.jp). The
    author received no specific funding for this work. Code and
    configs are released at [repository URL TBD].}%
}

\markboth{IEEE Transactions on Neural Networks and Learning Systems,
\,vol.~X,\,no.~X,\,Month~YYYY}%
{J.~Kobayashi: How a Predictive State Observer Can Self-Adapt Its Correction Gain}

\maketitle

\begin{abstract}
We ask whether a forward-model-based predictive state observer can
adapt its sensory prediction-error correction gain (hereafter,
\emph{correction gain}) from signals the agent itself has access to
--- innovation history and per-episode task outcome --- without any
offline oracle on the optimal gain. A residual MLP forward model
combines with a fixed-lag predictive state observer over a 34-muscle
MyoSuite reaching arm; the correction gain $K\!\in\![0,1]$ enters in
an innovation-style update. We propose a two-layer adaptation rule.
\emph{Within-trial}, a per-channel exponentially-weighted variance
of squared innovation gives a reliability estimate $r_f(t)$, and a
logistic $K_f(t)\!=\!\sigma(\beta_{0,f}\!+\!\beta_{1,f}\log r_f(t))$
maps it to a field-wise correction gain on the qpos, qvel, act,
tip\_pos, and reach\_err sensory channels.
\emph{Across-trial}, Simultaneous Perturbation Stochastic
Approximation (SPSA) updates the meta-parameters $\beta$ from the
per-episode reaching outcome. Plugging this observer into a
joint-space PD controller reveals four findings. \emph{First},
default within-trial reliability adaptation produces $K\!\approx\!
0.6$--$0.7$ across the focused noise--delay grid; under a multi-step
($H{=}8$) supervised forward model whose closed-loop optimum is
$K{=}0$, this rule is structurally misaligned and trails $K{=}0$ by
23--29\,cm. \emph{Second}, across-trial SPSA on a single delay-18
cell converges $\beta$ to a regime where field-wise $K$ specializes
(qpos most suppressed at $\beta_{0,\mathrm{qpos}}\!\approx\!-1.5$;
qvel slope $\beta_{1,\mathrm{qvel}}$ boosted to $0.9$) and the
closed-loop min-tip reaches $0.48$\,m, matching $K{=}0$ within
$1$\,cm. \emph{Third}, multi-cell SPSA over the full grid yields
only $3$--$5$\,cm improvement at delay-18 cells: cell-heterogeneous
reliability profiles resist a single global $\beta$. \emph{Fourth},
transferring the ReachFixed-learned $\beta$ to the variable-target
ReachRandom variant hurts; on ReachRandom, where the closed-loop
oracle $K^{\star}_{\mathrm{cl}}$ is multimodal across cells, the
default within-trial reliability rule alone already beats $K{=}0$ by
$1$--$3$\,cm at delay-18 cells. The right correction-gain rule is
therefore task-dependent: outcome-driven adaptation closes the gap
when the closed-loop optimum is uniform, while default reliability
adaptation is competitive when the optimum is heterogeneous.
Offline-oracle-supervised gain predictors
(Appendix~\ref{app:oracle-supervised}) serve as an upper-bound
diagnostic against which agent-available adaptation can be measured.
\end{abstract}

\begin{IEEEkeywords}
Forward model, predictive state observer, sensory prediction-error
correction, reliability-adaptive correction gain, outcome-driven
adaptation, SPSA, multi-step rollout supervision, closed-loop
control, MyoSuite, muscle-driven reaching.
\end{IEEEkeywords}

% ============================================================
\section{Introduction}
% ============================================================

State estimation in muscle-driven manipulation faces three coupled
challenges: sensorimotor delay, observation noise, and signal-
dependent motor noise. The internal-model view of biological motor
control answers these with a \emph{forward-model-based predictive
state observer}: an internal forward model uses efference copy to
predict the next state, and the observer corrects that prediction by
a scalar weight $K\!\in\![0,1]$ on the sensory prediction error
\cite{bernstein1967,wolpert2000}. $K{=}0$ trusts the forward
prediction (efference copy); $K{=}1$ trusts the sensor; intermediate
$K$ implements a sensory-prediction-error correction. The
innovation-update form is structurally familiar from Kalman
filtering, where the gain is derived from propagated covariance
estimates \cite{kalman1960,mehra1970,anderson1979optimal}; we do
\emph{not} propagate covariances and do not claim to implement a
full Kalman filter (Sec.\,\ref{sec:method-kalman}). Instead we treat
$K$ as a scalar (or field-wise vector) that controls how strongly
sensory prediction error corrects the current state estimate, and
ask: \emph{can the agent learn to set this gain from signals it has
access to itself --- innovation history and per-episode task outcome
--- without an offline oracle on the optimal gain?}

This question is biologically motivated. The nervous system does not
have access to a swept oracle $K^{\star}$ across noise and delay
regimes; it has access to its own prediction errors and, after
moving, to whether the limb arrived where intended. We therefore
propose a \emph{two-layer reliability-adaptive observer}.
\emph{Within-trial}, a per-channel exponentially-weighted variance
of squared innovation gives a running reliability estimate
$r_f(t)$, and a logistic
$K_f(t)\!=\!\sigma(\beta_{0,f}\!+\!\beta_{1,f}\log r_f(t))$ maps it
to a field-wise correction gain on the qpos, qvel, act, tip\_pos,
and reach\_err sensory channels. \emph{Across-trial}, Simultaneous
Perturbation Stochastic Approximation (SPSA) \cite{spall1992spsa}
updates the meta-parameters $\beta\!=\!\{\beta_{0,f},\beta_{1,f}\}$
from the per-episode reaching outcome. Neither layer accesses the
optimal $K$, the true state, or a swept oracle; both use only
signals the agent generates during reaching.

We instantiate this observer on a 34-muscle MyoSuite reaching arm
\cite{caggiano2022myosuite} running in the MuJoCo simulator
\cite{todorov2012mujoco} with a residual-MLP forward model trained
under $H$-step rollout supervision ($H{=}8$) and a fixed-lag
predictive state observer that handles configurable observation
delay ($d\!\in\!\{0,18\}$\,steps) and configurable per-field
Gaussian noise. A separate joint-space PD controller with a one-shot
inverse-kinematics target then \emph{deploys} the observer in
closed loop. As a diagnostic upper bound, we also retain an
offline-oracle-supervised correction-gain predictor (Appendix
\ref{app:oracle-supervised}), which uses a per-condition closed-loop
$K$-sweep to define $K^{\star}_{\mathrm{cl}}$ and is the strongest
non-agent-available baseline available in this setting.

Our contributions are summarised in four claims.

\begin{itemize}
\item \textbf{C1.} \emph{Default within-trial reliability adaptation
is sensible but task-misaligned.} With $\beta_{0,f}\!=\!0$ and
$\beta_{1,f}\!=\!0.5$ (a deliberately neutral prior), the
within-trial rule produces $K\!\approx\!0.6$--$0.7$ across the
focused $3$~noise $\times\,2$~delay grid. Under the $H{=}8$ forward
model on this task, however, the closed-loop oracle
$K^{\star}_{\mathrm{cl}}$ is $K{=}0$ at every cell, so default
reliability adaptation trails $K{=}0$ by 23--29\,cm in $\min$-tip
error (Fig.\,\ref{fig:F_reliability_default}). Innovation-based
reliability alone --- without an outcome signal --- cannot recover
the right level on this task.

\item \textbf{C2.} \emph{Single-cell across-trial SPSA closes the
gap.} Running SPSA on the per-episode $\min$-tip outcome on the
single $(\sigma{=}\mathrm{none},\,d{=}18)$ cell for $100$ iterations
($12$ samples/side, paired perturbations) drives $\beta$ to a
regime in which the field-wise correction gains specialize: qpos is
most suppressed ($\beta_{0,\mathrm{qpos}}\!\approx\!-1.5$) while
qvel's slope is boosted to $\beta_{1,\mathrm{qvel}}\!\approx\!0.9$.
On the training cell the closed-loop $\min$-tip falls to $0.48$\,m,
matching the $K{=}0$ baseline within $1$\,cm and far outperforming
default reliability ($0.77$\,m at the same cell). Outcome-driven
$\beta$ adaptation alone, with no oracle access, can recover
$K^{\star}_{\mathrm{cl}}$-equivalent performance.

\item \textbf{C3.} \emph{Multi-cell SPSA is bottlenecked by
cell heterogeneity.} Repeating the same SPSA on the full $6$-cell
grid (uniform random cell sampling per iteration) yields a balanced
single $\beta$ with $K_{\mathrm{mean}}\!\approx\!0.51$ at delay-18
cells and $\approx\!0.63$ at delay-0 cells. The resulting outcome
improvement at delay-18 cells is only $3$--$5$\,cm, and there is no
improvement at delay-0 cells. A single global $\beta$ cannot
simultaneously suppress $K$ where useful and elevate it elsewhere
unless $\beta_{0,f}$ is pushed deeply negative; the structural cost
of one-$\beta$-fits-all surfaces as residual outcome error
(Fig.\,\ref{fig:F_spsa_fullgrid}).

\item \textbf{C4.} \emph{Task transfer reveals the rule is
task-dependent.} On the variable-target \texttt{ReachRandom} variant
we find that $K^{\star}_{\mathrm{cl}}$ is multimodal across cells
($\{0, 0.25, 1\}$, Appendix~\ref{app:reach-random}); a single $K$ no
longer dominates. Transferring the ReachFixed-trained $\beta$ to
ReachRandom \emph{hurts} (pushes $K$ too low globally and worsens
$\min$-tip by $4$--$5$\,cm at delay-18), while the \emph{default}
within-trial reliability rule alone --- with no SPSA training of
$\beta$ --- already beats $K{=}0$ by $1$--$3$\,cm at delay-18 cells.
The right correction-gain rule is therefore task-dependent:
outcome-driven $\beta$ adaptation matches when
$K^{\star}_{\mathrm{cl}}$ is uniform; default reliability is
competitive when $K^{\star}_{\mathrm{cl}}$ is heterogeneous.
\end{itemize}

Together, C1--C4 say that the closed-loop correction gain can be
recovered from agent-available signals alone, but only when
the within-trial reliability rule, the across-trial outcome
objective, and the task's reliability--outcome geometry are in
agreement. They also expose a structural limitation of any
single-$\beta$ adaptation strategy on tasks where
$K^{\star}_{\mathrm{cl}}$ varies across noise--delay--target cells.
Appendix~\ref{app:oracle-supervised} reports an
offline-oracle-supervised correction-gain predictor as the strongest
non-agent-available baseline; that predictor is a useful upper-bound
diagnostic against which our agent-available adaptation can be
measured, but as a model of how a biological agent could set its
correction gain it requires access the agent does not have.

The rest of the paper is organised as follows.
Section~\ref{sec:related} positions our work against prior literature
on adaptive correction gains and Kalman-style updates, forward models
in motor control, learned dynamics, outcome-driven adaptation, and
muscle-driven simulation.
Section~\ref{sec:method} describes the task, the forward model, the
fixed-lag predictive state observer, the reliability-adaptive
correction-gain rule, the across-trial SPSA loop, the controller, and
the evaluation pipeline.
Section~\ref{sec:results} reports the four claims with the
corresponding figures.
Section~\ref{sec:discussion} discusses the trade-offs and the
implications of agent-available correction-gain adaptation for
biological motor control models.

% ============================================================
\section{Related Work}
\label{sec:related}
% ============================================================

\noindent\textbf{Adaptive correction gains and Kalman-style updates.}
The Kalman filter \cite{kalman1960} and its smoothing
counterpart~\cite{rauch1965} are the textbook framework for combining
a state-space model with noisy observations under known noise
covariances. When the covariances are unknown, the classical
\emph{adaptive Kalman filter} estimates them online from the
innovation sequence \cite{mehra1970}; the steady-state gain $K$
then drops out of the covariance estimate. We adopt the same
innovation-style update form (residual MLP for dynamics, fixed-lag
buffered correction for delays) but \emph{do not} propagate
covariances and do not compute an optimal Kalman gain; the
state-space formulation is standard textbook material
\cite{anderson1979optimal} and we treat the result as a predictive
state observer with a scalar correction gain (Sec.\,\ref{sec:method-kalman}).
The novelty of our setting is therefore not in the filter equations
but in the \emph{choice} of $K$: instead of deriving $K$ from a
noise-covariance estimate, we train a small condition-level predictor
that maps (controller, $\sigma$, delay) to a scalar $K$ supervised
against a swept oracle. The contribution we report is what happens
when that oracle is open-loop versus closed-loop, a distinction
largely absent from the adaptive-Kalman literature and not natural
inside the covariance-propagation framing.

\smallskip

\noindent\textbf{Forward / internal models in motor control.}
The view that the central nervous system maintains an internal forward
model of the limb to predict the sensory consequences of motor
commands traces back to Bernstein's degrees-of-freedom
analysis~\cite{bernstein1967} and was given an explicit computational
framing by Wolpert and Ghahramani~\cite{wolpert2000}, who identified
forward and inverse models as complementary processes underlying
sensorimotor estimation, prediction, and learning. Our residual MLP is
a coarse instantiation of the forward model in that picture; the
multi-step supervision we add is motivated by the fact that the loop
that consumes the estimate operates over hundreds of integration steps
and thus depends on long-horizon prediction quality. More biologically
grounded forward-model families --- continuous-time recurrent networks
with input-modulated time constants such as liquid time-constant
networks~\cite{hasani2021liquid} and their closed-form
variant~\cite{hasani2022cfc} --- have been proposed for sensorimotor
dynamics; in this work we keep the forward-model implementation
intentionally generic to isolate the oracle-choice question.

\smallskip

\noindent\textbf{Learned dynamics and multi-step supervision.}
Model-based reinforcement-learning systems have moved from one-step
dynamics losses to multi-step rollout objectives because closed-loop
planners need predictions that survive being unrolled. Dreamer
\cite{hafner2020dreamer} backpropagates through imagined trajectories
in a latent world model, and MBPO \cite{janner2019mbpo} branches
short model rollouts from real data, formalising ``when to trust your
model''. We take the same lesson into observer design: a forward
model that is accurate enough over the controller's planning horizon
shifts where prediction-error correction is helpful and where it is
harmful.

\smallskip

\noindent\textbf{Outcome-driven adaptation and SPSA.}
Outcome-driven, gradient-free adaptation has a long history in motor
control: trial-by-trial visuomotor adaptation in humans
\cite{wolpert2000} is captured by an internal-model update rule that
uses only the observed end-of-trial error, not the underlying
optimal-gain calculation. Algorithmically, our across-trial loop
follows Spall's Simultaneous Perturbation Stochastic Approximation
(SPSA)~\cite{spall1992spsa}: a two-point gradient estimate using a
single Rademacher perturbation per iteration, with a robust
hyperparameter schedule. SPSA's appeal in this setting is that it
needs only the per-episode outcome, not the per-step state, and that
its computational cost scales with the number of meta-parameters
$\beta$ (here, $10$) rather than the dimension of the observer
state (here, $83$). Behaviour cloning and DAgger
\cite{pomerleau1988alvinn, ross2011dagger} were used in an earlier
version of this work to ask whether the resulting controllers
consume state estimates; we found that with small demonstration sets
they do not (Appendix~\ref{app:oracle-supervised}), and we now use
only state-coupled feedback controllers in the main results so the
observer signal flows to muscle commands.

\smallskip

\noindent\textbf{Muscle-driven simulation.}
Our task runs in MuJoCo \cite{todorov2012mujoco} via the MyoSuite
musculoskeletal benchmark \cite{caggiano2022myosuite}, which provides
a 34-muscle reaching arm and a contact-rich set of tasks suitable for
sensorimotor estimation studies. MyoSuite is increasingly used as a
testbed for reinforcement-learning controllers; here we use it instead
to ask whether a predictive state observer can adapt its
correction-gain from agent-available signals alone, a question that
the RL-centric literature has not addressed.

% ============================================================
\section{Method}
\label{sec:method}
% ============================================================

\subsection{Task and Environment}
We use the \texttt{myoArmReachFixed-v0} task in MyoSuite\,2.12.2, a 34-muscle
shoulder-elbow-wrist-hand system actuated by activations in $[0,1]^{34}$.
The task is to drive the index-finger tip to a randomly drawn target in a
$12$\,s episode at $\Delta t{=}0.02$\,s (600 control steps). The flat state $x\!\in\!\mathbb{R}^{83}$ stacks joint positions
$q\!\in\!\mathbb{R}^{20}$, joint velocities $\dot q\!\in\!\mathbb{R}^{20}$,
muscle activations $a\!\in\!\mathbb{R}^{34}$, finger-tip position
$p_{\mathrm{tip}}\!\in\!\mathbb{R}^{3}$, target
$p_{\mathrm{tgt}}\!\in\!\mathbb{R}^{3}$, and reach error
$e{=}p_{\mathrm{tgt}}-p_{\mathrm{tip}}\!\in\!\mathbb{R}^{3}$. True state is
extracted from MuJoCo and used only as an oracle for evaluation; the closed
loop never accesses it (Fig.\,\ref{fig:system}).

\begin{figure*}[t]
\centering
% Define a small colour-blind-safe palette (ColorBrewer Set2 / Paired)
\definecolor{envCol}{HTML}{D9D9D9}    % grey
\definecolor{obsCol}{HTML}{A6CEE3}    % light blue
\definecolor{estCol}{HTML}{B2DF8A}    % light green
\definecolor{fwdCol}{HTML}{FDBF6F}    % light orange
\definecolor{ctlCol}{HTML}{CAB2D6}    % light violet
\definecolor{evalCol}{HTML}{FB9A99}   % light red (dashed eval box)
\definecolor{actCol}{HTML}{6A3D9A}    % deep violet (action arrow)
\definecolor{lblBg}{HTML}{FFFFFF}     % label background
\begin{tikzpicture}[
    >={Stealth[length=2.2mm,inset=0.8mm]},
    box/.style={draw=black!75, rounded corners=2.5pt, minimum height=11mm,
                align=center, font=\footnotesize, inner sep=3pt,
                line width=0.5pt},
    env/.style={box, fill=envCol,  minimum width=21mm},
    obs/.style={box, fill=obsCol,  minimum width=24mm},
    est/.style={box, fill=estCol,  minimum width=33mm, minimum height=15mm},
    fwd/.style={box, fill=fwdCol,  minimum width=29mm, font=\scriptsize,
                minimum height=10mm},
    ctl/.style={box, fill=ctlCol,  minimum width=22mm},
    eval/.style={box, dashed, draw=evalCol!80!black, fill=evalCol!30,
                 minimum width=29mm, font=\scriptsize\itshape},
    lbl/.style={font=\scriptsize, fill=lblBg, inner sep=1pt},
    arrlbl/.style={lbl, midway},
    flowarr/.style={->, line width=0.55pt},
    actarr/.style={->, line width=0.8pt, color=actCol},
    evalarr/.style={->, dashed, color=evalCol!70!black, line width=0.5pt},
    node distance=6mm and 12mm,
]
% --- Main horizontal flow ---
\node[env] (env) {MuJoCo /\\MyoSuite};
\node[obs, right=of env] (obs) {Obs.\ wrappers\\noise $+$ delay};
\node[est, right=of obs] (est) {State observer\\
        $\hat x_t {=} x_{\mathrm{pred}} {+} K\!\cdot\!(y_t{-}x_{\mathrm{pred}})$};
\node[ctl, right=of est] (ctl) {Controller\\PD / BC};

% --- Forward model below estimator ---
\node[fwd, below=11mm of est] (fwd)
    {forward model $f_\theta$\\(residual MLP)};

% --- Evaluation-only oracle (sits beside env, not inside the loop) ---
\node[eval, below=11mm of env] (eval)
    {true state $x_t$\\(oracle, eval only)};

% --- Flow arrows ---
\draw[flowarr] (env) -- node[arrlbl,above]{$x_t$} (obs);
\draw[flowarr] (obs) -- node[arrlbl,above]{$y_t$} (est);
\draw[flowarr] (est) -- node[arrlbl,above]{$\hat x_t$} (ctl);

% --- Forward-model bidirectional connection ---
% Up arrow on the left -- forward model returns x_pred.
\draw[flowarr] ($(fwd.north west) + (8mm,0)$)
    -- node[lbl,pos=0.45]{$x_{\mathrm{pred}}$}
    ($(est.south west) + (8mm,0)$);
% Down arrow on the right -- estimator feeds (x_hat,u) at last step.
\draw[flowarr] ($(est.south east) + (-8mm,0)$)
    -- node[lbl,pos=0.55]{$\hat x_{t{-}1},\, u_{t{-}1}$}
    ($(fwd.north east) + (-8mm,0)$);

% --- Closed-loop action arrow (controller -> env, routed underneath) ---
\draw[actarr] (ctl.south) -- ++(0,-25mm) coordinate (botctl)
    -| node[arrlbl,below=1mm,pos=0.92]
        {$u_t \in [0,1]^{34}$\,\textit{(muscle exc.)}}
    (env.south);

% --- Eval arrow (oracle path; visually subdued) ---
\draw[evalarr] (env.south) -- node[lbl,midway,fill=white]{oracle}
    (eval.north);

% --- Background dashed box for closed loop ---
\begin{scope}[on background layer]
\node[draw=black!35, dashed, rounded corners,
      fit=(env)(obs)(est)(ctl)(fwd),
      inner sep=4mm,
      label={[font=\scriptsize\itshape,anchor=north east]north east:closed
             loop (no true state)}] {};
\end{scope}
\end{tikzpicture}
\caption[Closed-loop pipeline.]%
{\textbf{Closed-loop pipeline.} Each control step, the env's true
state $x_t$ is filtered through observation wrappers that add
per-field Gaussian noise and a fixed delay, producing $y_t$. The
predictive state observer combines $y_t$ with a one-step
prediction $x_{\mathrm{pred}}$ produced by the residual-MLP forward
model $f_\theta(\hat x_{t-1}, u_{t-1})$ via a scalar correction gain
$K\!\in\![0,1]$ on the sensory prediction error $y_t - x_{\mathrm{pred}}$
(Eq.\,\ref{eq:kalman_update}). The controller consumes only the
observer output $\hat x_t$ and emits the next muscle excitation
$u_t\!\in\![0,1]^{34}$. The true state $x_t$ is exposed only to the
offline evaluator (dashed red box); the closed loop never accesses
it.}
\label{fig:system}
\end{figure*}

\subsection{Forward Model}
A residual MLP $f_\theta(x_t, u_t)$ predicts the state delta
$\Delta x_t {=} x_{t+1}-x_t$ rather than $x_{t+1}$ directly. At
$\Delta t{=}0.02$\,s the state changes slowly, so $x_{t+1}\!\approx\!x_t$
component-wise; predicting $\Delta x$ frees the model from re-learning
the identity map and concentrates capacity on the small per-step
dynamics that the filter and controller actually consume. The
parameterisation is standard in model-based RL world
models~\cite{hafner2020dreamer,janner2019mbpo}. The input is the flat
83-dim state and the 34-dim excitation; the architecture is two hidden
layers of 256 units with ReLU and LayerNorm; training uses
Adam~\cite{kingma2015adam}.
In the single-step baseline, $f_\theta$ is trained on the standard
one-step MSE loss
\begin{equation}
\mathcal{L}_1(\theta) = \mathbb{E}_{(x_t,u_t,x_{t+1})}\big[\|f_\theta(x_t,u_t)
- \Delta x_t\|^2\big].
\label{eq:L_one_step}
\end{equation}

\noindent\textbf{Multi-step rollout supervision.} The multi-step
variant trains $f_\theta$ with an $H$-step rollout loss instead of
the one-step MSE, where $H$ denotes the training rollout horizon
(distinct from the correction gain $K$ defined in
Sec.\,\ref{sec:method-kalman}). For each contiguous trajectory chunk
$(x_t,u_t,\dots,x_{t+H})$ inside a single source episode, the model
is unrolled free-running:
$\hat x_{t+k} = \hat x_{t+k-1} + f_\theta(\hat x_{t+k-1}, u_{t+k-1})$,
$\hat x_t = x_t$, and the loss averages MSE across the $H$
prediction steps:
\begin{equation}
\mathcal{L}_H(\theta) = \mathbb{E}\Big[\tfrac{1}{H}\sum_{k=1}^H\|\hat x_{t+k}
- x_{t+k}\|^2\Big].
\label{eq:L_H_step}
\end{equation}
Multi-step rollout supervision has been used to stabilise long-horizon
predictions in latent-imagination world models~\cite{hafner2020dreamer}
and in model-based policy optimisation~\cite{janner2019mbpo}, where the
relevant horizon is the planner's rollout depth rather than the
observer's delay; we re-use the technique here to make $f_\theta$
robust across the observer's buffer rollout.

\noindent We train the same architecture with $H\!\in\!\{1,4,8\}$ on the
same expanded dataset (51\,k transitions across 106 episodes; see
Sec.\,\ref{sec:results-3.1}).

\subsection{Fixed-Lag Predictive State Observer}
\label{sec:method-kalman}
\emph{We do not claim to implement a full Kalman filter.} The
observer below uses an innovation-update form structurally familiar
from Kalman filtering, but it does not propagate covariance matrices
$P$, $Q$, $R$ and does not compute an optimal Kalman gain. We use
the form because it provides a controlled, scalar way to vary the
reliance on sensory feedback versus forward prediction; the gain
$K\!\in\![0,1]$ here is a \emph{sensory prediction-error correction
gain}, not a covariance-derived Kalman gain.

The observation pipeline produces $y_t = h(x_{t-d}) + \eta_t$ where
$h$ is the identity (the state vector and the observation share the
same schema), $d\!\in\!\mathbb{Z}_{\geq 0}$ is a fixed delay in
control steps, and $\eta_t$ is per-field i.i.d.\ Gaussian noise with
diagonal covariance $\mathrm{diag}(\sigma^2)$. The estimator maintains
a length-$(d{+}1)$ ring buffer of past one-step estimates
$(\hat x_{t-d}, \dots, \hat x_{t-1})$ together with the corresponding
applied actions $(u_{t-d}, \dots, u_{t-1})$. At each step the
observer first runs the forward model on every buffered estimate to
obtain a prediction at the present time,
\begin{equation}
\tilde x_t = \hat x_{t-1} + f_\theta(\hat x_{t-1}, u_{t-1}),
\end{equation}
then forms an innovation against the \emph{delayed} buffer entry and
applies an element-wise gain $K\!\in\![0,1]^{83}$:
\begin{equation}
\hat x_{t-d}
\leftarrow
\hat x_{t-d} + K \odot \bigl(y_t - \hat x_{t-d}\bigr),
\label{eq:kalman_update}
\end{equation}
and finally re-rolls the corrected past estimate forward through the
buffered actions to obtain the present-time estimate $\hat x_t$.
Eq.\,(\ref{eq:kalman_update}) collapses to the standard one-step
innovation update when $d{=}0$. Throughout the paper $K$ is a scalar
broadcast across the 83 fields so the correction gain reduces to a
single trust knob between forward prediction ($K{=}0$) and
observation ($K{=}1$). This fixed-lag construction follows the
buffered Rauch--Tung--Striebel form~\cite{rauch1965,anderson1979optimal}
specialised to a scalar gain.

\subsection{Reliability-Adaptive Correction Gain (Within-Trial)}
\label{sec:method-reliability}
The observer above uses a fixed scalar $K$ broadcast across all 83
fields. We propose instead an \emph{agent-available, field-wise
reliability-adaptive} rule that sets $K_f(t)$ online from each
sensory channel's own innovation history. No oracle on the optimal
gain, no true state, and no swept-condition labels enter the rule;
only the per-step innovation $e_t \!=\! y_t \!-\! \hat x_{t-d}$ does.

We partition the 83-dim state into five \emph{sensory fields}: qpos
(20), qvel (20), act (34), tip\_pos (3), and reach\_err (3). The
target-position channel (3) is held at a fixed gain because the
target is known and does not have a meaningful prediction error.
For each field $f\!\in\!\mathcal{F}\!=\!\{\mathrm{qpos},
\mathrm{qvel}, \mathrm{act}, \mathrm{tip\_pos},
\mathrm{reach\_err}\}$ the observer maintains an exponentially-
weighted (EMA) estimate of the per-field squared-innovation mean,
\begin{equation}
v_f(t) = (1-\alpha)\,v_f(t-1) + \alpha\;
        \frac{1}{|\mathcal{I}_f|}\sum_{i \in \mathcal{I}_f} e_{t,i}^{2},
\label{eq:ema_innovation_var}
\end{equation}
where $\mathcal{I}_f$ are the field's coordinate indices in the
83-dim state vector and $\alpha\!=\!0.05$ unless otherwise noted.
The agent's running estimate of \emph{sensory reliability} for
field $f$ is the inverse of this variance,
\begin{equation}
r_f(t) = \frac{1}{\varepsilon + v_f(t)},
\label{eq:reliability}
\end{equation}
with $\varepsilon\!=\!10^{-6}$. We then map reliability to a
per-field correction gain through a two-parameter logistic,
\begin{equation}
K_f(t) = \sigma\bigl(\beta_{0,f} + \beta_{1,f}\,\log r_f(t)\bigr),
\quad \sigma(z)\!=\!\frac{1}{1+e^{-z}},
\label{eq:Kf_logistic}
\end{equation}
and form the 83-dim gain vector $K(t)\!\in\![0,1]^{83}$ by
broadcasting $K_f(t)$ over its field's coordinates. The
innovation update Eq.\,(\ref{eq:kalman_update}) is then applied
elementwise with $K(t)$ in place of the scalar $K$.

Eq.\,(\ref{eq:Kf_logistic}) is intentionally simple. The slope
$\beta_{1,f}$ controls how sharply the gain reacts to changes in
log-reliability, and the intercept $\beta_{0,f}$ controls the
operating point at $r_f(t)\!=\!1$ (i.e., $K_f\!=\!\sigma(\beta_{0,f})$).
With $\beta_{0,f}\!=\!0$, $\beta_{1,f}\!=\!0.5$, and $v_f(0)\!=\!1$,
the rule produces $K_f(t)\!\approx\!0.5$ on the first step and
adapts upward when innovation shrinks (the sensor is informative) or
downward when innovation grows (the sensor is noisy). Per-field
$\beta\!=\!\{\beta_{0,f}, \beta_{1,f}\}$ lets the rule learn that
different fields have different reliability--correction trade-offs
under the same physical noise level.

\subsection{Across-Trial Outcome Adaptation (SPSA)}
\label{sec:method-spsa}
The meta-parameters $\beta\!=\!\{(\beta_{0,f}, \beta_{1,f}):
f\!\in\!\mathcal{F}\}\!\in\!\mathbb{R}^{10}$ themselves can be
adapted across trials from \emph{outcome} signals the agent has at
the end of each episode. We define the per-episode outcome as the
minimum tip-to-target distance,
\begin{equation}
\mathrm{minTip}(\beta;\,\sigma,d) \;=\;
   \min_{t\in[0,T]}\,\|p_{\mathrm{tip}}(t) - p_{\mathrm{tgt}}\|,
\label{eq:outcome}
\end{equation}
which the agent observes by proprioception and visual feedback
after the reach. The across-trial loop minimises
$\mathbb{E}[\mathrm{minTip}(\beta)]$ where the expectation is over
target draws (and, in the multi-cell variant, over uniformly drawn
$(\sigma, d)$ cells from the focused grid).

Because the closed-loop $\mathrm{minTip}$ is not differentiable in
$\beta$ (it passes through env steps, EMA state, and a non-smooth
$\min$), we use Simultaneous Perturbation Stochastic Approximation
(SPSA)~\cite{spall1992spsa}: at iteration $n$, draw a Rademacher
vector $\Delta_n\!\in\!\{\pm 1\}^{10}$, evaluate the outcome at
$\beta_n \pm c_n \Delta_n$ averaged over $S$ paired episodes
(samples per side), form the central-difference gradient estimate
\begin{equation}
\hat g_n = \frac{
   \mathrm{minTip}(\beta_n + c_n\Delta_n)
   - \mathrm{minTip}(\beta_n - c_n\Delta_n)
}{2 c_n}\,\Delta_n^{-1},
\label{eq:spsa_grad}
\end{equation}
and update $\beta_{n+1} \!=\! \beta_n - a_n \hat g_n$. The Spall
schedules are $a_n \!=\! a / (n{+}1{+}A)^{\alpha_s}$ and
$c_n \!=\! c / (n{+}1)^{\gamma_s}$ with $a{=}2.0$, $c{=}0.3$,
$\alpha_s{=}0.602$, $\gamma_s{=}0.101$, and $A{=}5$. Two SPSA
variants are reported: \emph{single-cell} ($S{=}12$, $100$
iterations, $\sigma{=}\mathrm{none}$, $d{=}18$) and \emph{full-grid}
($S{=}12$, $100$ iterations, uniformly drawn $(\sigma,d)$ from the
$6$-cell grid per iteration).

\paragraph{Comparison with oracle-supervised gain.}
For a diagnostic upper bound on what \emph{any} $K$-setting strategy
can achieve on this task, Appendix~\ref{app:oracle-supervised}
reports an offline-oracle-supervised correction-gain predictor that
trains a small MLP $g_\phi(\sigma, d, c)\!\to\![0,1]$ against either
an open-loop estimation-accuracy oracle
$K^{\star}_{\mathrm{ol}}(\sigma, d, c)$ or a closed-loop
task-error oracle
$K^{\star}_{\mathrm{cl}}(\sigma, d, c)\!=\!\arg\min_{K}\mathbb{E}[
\mathrm{minTip}(K)\!\mid\!\sigma,d,c]$. That predictor requires
access to a swept $K$-grid the biological agent does not have; we
include it strictly as the strongest non-agent-available baseline.

\subsection{Controllers}
Three controllers are evaluated as closed-loop wrappers around the
estimator.

\emph{(i)~Heuristic projection.}
A fixed random projection $W\!\in\!\mathbb{R}^{34\times 3}$ (drawn
once with a fixed seed from $\mathcal{N}(0,1)^{34\times 3}$) maps the
estimated 3-dim reach-error $\hat r$ to a 34-dim muscle excitation
through an element-wise logistic $\mathrm{sigm}(\cdot)$ (distinct from
the noise scale vector $\sigma$ above):
\begin{equation}
u = \mathrm{sigm}\!\left(b - g\,W\,\hat r\right),
\quad b{=}0,\ g{=}5,
\label{eq:heur}
\end{equation}
producing a state-sensitive baseline that does not actually reach.

\emph{(ii)~Joint-space PD with inverse kinematics.}
For each target tip position $p^{\star}\!\in\!\mathbb{R}^{3}$ we solve
$q^{\star} = \mathrm{IK}(p^{\star})$ once at episode start via a
damped-Jacobian Newton iteration
\begin{equation}
q^{(k+1)} = q^{(k)} + J^{\top}\!\left(J J^{\top} + \lambda^{2} I\right)^{-1}\!
            \bigl(p^{\star} - p(q^{(k)})\bigr),
\label{eq:ik}
\end{equation}
where $J\!=\!\partial p / \partial q$ is the tip-site position
Jacobian obtained from MuJoCo's \texttt{mj\_jacSite} and
$\lambda^{2}{=}0.1$ is the damping. The arm is then driven by a
joint-space PD law and a moment-arm muscle map:
\begin{align}
u_{\text{joint}}  &= K_p\,(q^{\star} - \hat q) - K_d\,\hat{\dot q}, \label{eq:pd}\\
u_{\text{muscle}} &= \mathrm{clip}\!\left(\alpha\,\mathrm{ReLU}\!
                     \left(M\,u_{\text{joint}}\right),\;0,\,1\right),
                     \label{eq:muscle_map}
\end{align}
where $M\!\in\!\mathbb{R}^{34\times 20}$ is the muscle-tendon
moment-arm matrix (the dense form of MuJoCo's sparse
\texttt{actuator\_moment} at the env's current pose), and
$\alpha\!=\!5$ is a scalar action scale. Tuned gains:
$K_p\!=\!30$, $K_d\!=\!3$.

\emph{(iii)~Endpoint-error sensorimotor policy.}
A task-level analogue of (ii) that skips the IK pre-step and maps the
estimated tip-to-target error to muscle excitation via a Cartesian PD
law projected through the tip Jacobian and the moment-arm matrix:
\begin{align}
u_{\mathrm{tip}}   &= K_{p,e}\,(p^{\star}-\hat p_{\mathrm{tip}}) - K_{d,e}\,J\hat{\dot q},\\
u_{\mathrm{joint}} &= J^{\top}\,u_{\mathrm{tip}},\\
u_{\mathrm{muscle}}&= \mathrm{clip}\!\bigl(\alpha\,\mathrm{ReLU}(M\,u_{\mathrm{joint}}),\,0,\,1\bigr),
\label{eq:endpoint_fb}
\end{align}
with $K_{p,e}{=}30$, $K_{d,e}{=}3$, $\alpha{=}5$ and $J$ the tip-
site positional Jacobian captured at the env's neutral pose. We use
this as a transparent task-level state-coupled policy that lets the
observer output flow directly to muscle commands without an IK pre-
step; the $J^{\top}$-style projection is the classic Jacobian-
transpose method and is \emph{not} an optimal feedback controller
(no Riccati / LQR / OFC is solved here).

\emph{(iv)~Behaviour-cloned MLP (diagnostic only).}
A two-hidden-layer (256-unit) sigmoid policy trained on 30 scripted
IK demonstrations \cite{pomerleau1988alvinn} (no
DAgger~\cite{ross2011dagger}). We retain this controller only as a
diagnostic in Appendix~\ref{app:oracle-supervised} to test whether
a learned downstream policy consumes the observer signal; the main
results of Sec.\,\ref{sec:results-c1}--\ref{sec:results-c4} use
joint-PD (ii) so the observer output flows to muscle commands
through an explicit feedback law.

\subsection{Evaluation Pipeline}
\emph{Open-loop} evaluation replays a recorded episode and applies
observation wrappers to synthesise $y_t$ for an arbitrary $(\sigma, d)$
combination; the estimator is then run on the synthesised stream and
per-field state error is aggregated. \emph{Closed-loop} evaluation drives
the actual env: $u_t$ comes from the controller, which sees the estimator
output (never the true state); 10 episodes per cell, fixed seed per episode.
The same evaluation grid (3 controllers $\times$ 6 noise levels
$\times$ 4 delays $=72$ open-loop conditions; the focused closed-loop
grid is 3 noise levels $\times$ 2 delays $\times$ 10 episodes
$=60$ episodes per estimator) is reused across phases.

% ============================================================
\section{Results}
\label{sec:results}
% ============================================================

\subsection{Forward-model improvement cycle}
\label{sec:results-3.1}

An initial forward model trained on the original $\sim 3.6$\,k
transitions from 6 episodes had low single-step error but unbounded
long-horizon rollouts ($h{=}50$ MSE $\sim 10.6$). Collecting 100
additional \texttt{random} and \texttt{lowamp} episodes (a total of
$51\,$k transitions across 106 episodes) and retraining the same
architecture cut $h{=}50$ MSE to 0.052 --- a $\sim\!200\times$
improvement. We use this expanded dataset, and the resulting model,
as the single-step baseline throughout the paper.

\subsection{Default within-trial reliability is task-misaligned (C1)}
\label{sec:results-c1}

We first evaluate the reliability-adaptive observer with a
deliberately neutral default $\beta$: $\beta_{0,f}\!=\!0$,
$\beta_{1,f}\!=\!0.5$ for all five fields, $\alpha\!=\!0.05$,
$v_f(0)\!=\!1$. At iteration~$0$ this produces
$K_f\!\approx\!\sigma(0)\!=\!0.5$; as the EMA tracks each field's
running squared innovation, $K_f$ adapts upward when the channel
becomes informative and downward when it becomes noisy.

Across the focused $3$~noise~$\times\,2$~delay grid under the
$H{=}8$ forward model, the resulting per-field correction gains
settle near $K_f\!\approx\!0.55$--$0.72$ in the second half of each
episode, with the largest field-to-field spread of $\sim\!0.15$ in
the high-noise cells (qpos lowest, qvel and reach\_err highest).
Closed-loop $\min$-tip averages $0.766$\,m across the six cells.
For comparison, $K{=}0$ averages $0.487$\,m and $K{=}1$ averages
$0.701$\,m on the same grid.

The structural reason is that under the $H{=}8$ forward model the
closed-loop oracle on this task is $K^{\star}_{\mathrm{cl}}\!=\!0$
in every cell of the focused grid (Appendix~\ref{app:cl-sweep}): the
forward model is accurate enough over the controller's planning
horizon that any observation correction shifts the trajectory away
from the planned IK target. Reliability-based adaptation \emph{by
construction} cannot produce $K_f\!=\!0$ unless either
$\beta_{0,f}\!\to\!-\infty$ or the EMA variance grows without bound;
under bounded innovation neither happens. The default rule
therefore trails $K{=}0$ by $0.23$--$0.29$\,m in $\min$-tip across
the grid. We read this as a fundamental signal: \emph{innovation
reliability alone, without an outcome objective that knows the task,
cannot recover the right correction-gain level when the forward
model is good enough to operate the loop without sensory help.}

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{F_reliability_default.pdf}
\caption[Default within-trial reliability is task-misaligned.]%
{\textbf{Closed-loop $\min$-tip error under the $H{=}8$ forward
model and joint-PD controller for three estimators on the focused
$3$~noise~$\times\,2$~delay grid (mean $\pm$ std over $10$ episodes
per cell).} The default reliability-adaptive rule
($\beta_{0,f}\!=\!0$, $\beta_{1,f}\!=\!0.5$, all fields) settles to
$K_f\!\approx\!0.55$--$0.72$ and trails $K{=}0$ by $23$--$29$\,cm
across cells; $K{=}1$ trails $K{=}0$ by $14$--$28$\,cm. The
closed-loop oracle $K^{\star}_{\mathrm{cl}}$ on this grid is $K{=}0$
at every cell (Appendix~\ref{app:cl-sweep}); a reliability rule
cannot produce $K_f{=}0$ from bounded innovation alone.}
\label{fig:F_reliability_default}
\end{figure}

\subsection{Single-cell SPSA recovers $K^{\star}_{\mathrm{cl}}$-equivalent performance (C2)}
\label{sec:results-c2}

We next add the across-trial outcome loop of
Sec.\,\ref{sec:method-spsa}. We run SPSA for $100$ iterations on the
single $(\sigma{=}\mathrm{none},\,d{=}18)$ cell with $S\!=\!12$
samples per side and the Spall schedule of
Sec.\,\ref{sec:method-spsa}. The outcome objective is the per-
episode $\min$-tip averaged across the $S$ paired episodes at the
two perturbed $\beta$ points.

Figure~\ref{fig:F_spsa_single} reports the outcome trajectory and
the final-iteration per-field $\beta$. The outcome falls from
$0.55$\,m at iteration~$1$ to $0.48$\,m at iteration~$100$, matching
the $K{=}0$ baseline ($0.47$\,m at this cell, computed from the
closed-loop $K$-sweep in Appendix~\ref{app:cl-sweep}) within
$1$\,cm. The final $\beta$ is non-uniform across fields: qpos has
$\beta_{0,\mathrm{qpos}}\!\approx\!-1.5$ (deepest suppression),
while qvel has $\beta_{1,\mathrm{qvel}}\!\approx\!0.9$ (steepest
reliability slope), and act / tip\_pos / reach\_err take
intermediate values
($\beta_{0,f}\!\in\![-0.6, 0.1]$, $\beta_{1,f}\!\in\![0.3, 0.6]$).
The realised per-field $K_f$ averaged over the last $1/3$ of the
trained episode is $K_{\mathrm{qpos}}\!\approx\!0.20$,
$K_{\mathrm{qvel}}\!\approx\!0.41$, $K_{\mathrm{act}}\!\approx\!0.55$,
$K_{\mathrm{tip\_pos}}\!\approx\!0.42$,
$K_{\mathrm{reach\_err}}\!\approx\!0.46$. Field specialisation
emerges directly from outcome feedback.

The key implication is that the agent does not need an oracle on
$K^{\star}_{\mathrm{cl}}$: from the same set of per-episode reach
outcomes a biological agent would have access to, SPSA on a $10$-
parameter $\beta$ is sufficient to converge to the level of the
closed-loop oracle within $1$\,cm on a single training cell.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{F_spsa_single.pdf}
\caption[Single-cell SPSA convergence.]%
{\textbf{Single-cell SPSA on $(\sigma{=}\mathrm{none},\,d{=}18)$
under the $H{=}8$ forward model and joint-PD controller.}
\emph{Top}: per-iteration $\min$-tip outcome (mean over the $S{=}12$
paired episodes per side); the dashed line marks the closed-loop
$K{=}0$ baseline on this cell ($0.47$\,m). The outcome falls from
$0.55$\,m to $0.48$\,m over $100$ SPSA iterations.
\emph{Bottom}: final per-field $\beta$ vector. qpos has the
most-negative intercept ($-1.5$), qvel the steepest slope ($0.9$);
the resulting average per-field correction gains $K_f$ specialise.}
\label{fig:F_spsa_single}
\end{figure}

\subsection{Multi-cell SPSA is bottlenecked by cell heterogeneity (C3)}
\label{sec:results-c3}

A single-cell-trained $\beta$ is task-specific. To test whether a
single $\beta$ can serve the whole focused grid, we repeat the SPSA
loop with each iteration's $S\!=\!12$ paired episodes drawn
uniformly across the $6$~cells. The outcome objective is the
$6$-cell mean $\min$-tip.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{F_spsa_fullgrid.pdf}
\caption[Multi-cell SPSA on the full grid.]%
{\textbf{Multi-cell SPSA over the focused $6$-cell grid under the
$H{=}8$ forward model and joint-PD controller.} \emph{Top}: per-
iteration $6$-cell-mean $\min$-tip outcome (mean over $S{=}12$
paired draws). \emph{Bottom}: per-cell $\min$-tip at iteration $100$
for $K{=}0$, default-reliability, and the multi-cell-trained $\beta$.
Improvement over default reliability is $3$--$5$\,cm at delay-18
cells and $\sim 0$\,cm at delay-0 cells; the multi-cell $\beta$
still trails $K{=}0$ by $13$--$19$\,cm at delay-18 cells.}
\label{fig:F_spsa_fullgrid}
\end{figure}

After $100$ iterations the multi-cell $\beta$ converges to
$\beta_0\!=\!\{$qpos:$-0.64$, qvel:$-0.21$, act:$0.12$,
tip\_pos:$-0.32$, reach\_err:$-0.16\}$,
$\beta_1\!=\!\{$qpos:$0.20$, qvel:$0.80$, act:$0.54$,
tip\_pos:$0.63$, reach\_err:$0.33\}$, with $K_{\mathrm{mean}}$
averaged across fields equal to $\approx\!0.51$ at delay-$18$ cells
and $\approx\!0.63$ at delay-$0$ cells. The realised closed-loop
$\min$-tip improves over default reliability by $3$--$5$\,cm at the
three delay-$18$ cells and by $\sim\!0$\,cm at the three delay-$0$
cells. Compared to $K{=}0$, the multi-cell $\beta$ still trails by
$13$--$19$\,cm at delay-$18$ cells and by $\sim\!8$\,cm at
delay-$0$ cells.

The structural reason is that $K^{\star}_{\mathrm{cl}}\!=\!0$
uniformly across the focused grid (Appendix~\ref{app:cl-sweep}),
yet field-wise reliability under $H{=}8$ has different absolute
levels and time courses at each cell. A single $\beta$ would need
$\beta_{0,f}$ deeply negative (a few standard deviations below the
realised $\log r_f(t)$ range) to drive every cell's $K_f$ near $0$;
SPSA does not reach this regime in $100$ iterations because each
intermediate $\beta_0$ value is already differentiable improvement
in some cells and worse in others. The cell-heterogeneity bottleneck
is therefore not an SPSA failure --- the single-cell run of C2
converged --- but a fundamental limit of a single global $\beta$
when the reliability geometry varies across cells.

\subsection{Task transfer reveals task-dependence (C4)}
\label{sec:results-c4}

The previous results all use ReachFixed, where the same target
location is reused across episodes. We test the same observer on
the \emph{variable-target} variant \texttt{myoArmReachRandom-v0},
where the target is freshly sampled per episode from the workspace
distribution. The forward model for this transfer slice is also
trained on ReachRandom rollouts (Appendix~\ref{app:reach-random})
so the forward-model accuracy axis is matched.

\begin{table}[!htbp]
\centering
\caption{Transfer of the reliability-adaptive observer to
ReachRandom (variable target). Mean $\min$-tip (m) over $10$
episodes per cell under the $H{=}8$ ReachRandom forward model and
joint-PD controller. ``default'' is the $\beta_{0,f}{=}0$,
$\beta_{1,f}{=}0.5$ rule; ``transferred'' is the ReachFixed full-
grid-SPSA-learned $\beta$ from C3 applied without retraining.}
\label{tab:reachrandom_transfer}
\setlength{\tabcolsep}{5pt}
\footnotesize
\begin{tabular}{@{}ll cccc@{}}
\toprule
noise & delay & $K{=}0$ & $K{=}1$ & default & transferred \\
\midrule
none  & 0  & 0.484 & 0.766 & 0.766 & 0.766 \\
none  & 18 & 0.486 & 0.473 & 0.466 & 0.516 \\
high  & 0  & 0.484 & 0.766 & 0.766 & 0.766 \\
high  & 18 & 0.486 & 0.474 & 0.471 & 0.520 \\
xhigh & 0  & 0.484 & 0.768 & 0.766 & 0.766 \\
xhigh & 18 & 0.486 & 0.481 & 0.461 & 0.523 \\
\bottomrule
\end{tabular}
\end{table}

Table~\ref{tab:reachrandom_transfer} reports the result. On
ReachRandom the closed-loop oracle $K^{\star}_{\mathrm{cl}}$ takes
three distinct values across the grid (Appendix~\ref{app:reach-random},
Table~\ref{tab:rr_h8_ksweep}): $K{=}0$ at the three
delay-$0$ cells, $K{=}0.25$ at (none, $d{=}18$) and (high, $d{=}18$),
and $K{=}1$ at (xhigh, $d{=}18$). At delay-$18$ cells, the default
reliability rule \emph{beats $K{=}0$} by $1.5$--$2.5$\,cm because
the reliability-driven $K_f$ in the $0.5$--$0.7$ range lies closer
to the per-cell $K^{\star}_{\mathrm{cl}}$ of $0.25$ than $K{=}0$
does. The transferred ReachFixed-trained $\beta$, in contrast,
pushes $K_f$ globally lower (mean $\approx\!0.51$ on ReachFixed,
$\approx\!0.30$ on ReachRandom) and trails $K{=}0$ by
$3$--$4$\,cm at delay-$18$ cells.

Two findings combine here. \emph{First}, on a task whose
$K^{\star}_{\mathrm{cl}}$ is heterogeneous, agent-available default
reliability is already competitive --- no outcome adaptation needed.
\emph{Second}, $\beta$ trained on a different task does not transfer
when the source task's $K^{\star}_{\mathrm{cl}}$ is uniform but the
target task's is heterogeneous: the source task pushes $\beta$ in a
direction that is harmful on the target. This is direct evidence
that the correction-gain rule must be sensitive to the
\emph{distribution of the closed-loop optimum}, not merely to the
noise and delay regime that within-trial reliability sees.

\subsection{Comparison with offline-oracle-supervised gain}
\label{sec:results-oracle-diagnostic}

For a diagnostic upper bound we compare against an offline-oracle-
supervised correction-gain predictor (Appendix~\ref{app:oracle-supervised}):
a small MLP $g_\phi(\sigma,d,c)$ trained against the open-loop
oracle $K^{\star}_{\mathrm{ol}}$ and, separately, against the
closed-loop oracle $K^{\star}_{\mathrm{cl}}$ obtained by a per-cell
$K$-sweep. On the focused grid under $H{=}8$ the closed-loop-oracle
variant outputs $K\!\approx\!0.02$ and matches the $K{=}0$ baseline
within $1.5$\,cm at all delay-18 cells; the open-loop-oracle variant
outputs $K\!\approx\!1$ and trails $K{=}0$ by $14$--$28$\,cm. The
closed-loop-oracle variant is therefore the strongest non-agent-
available baseline available in this setting, and the C2 single-cell
SPSA result effectively matches it on its training cell using only
agent-available signals.

% ============================================================
\section{Discussion}
\label{sec:discussion}
% ============================================================

\noindent\textbf{Innovation alone is not enough; outcome closes the
gap.} The C1 finding is the central structural observation: a
within-trial reliability rule that uses only the per-channel
innovation history settles on $K_f\!\approx\!0.6$--$0.7$ across the
focused grid, while the closed-loop oracle on the same grid is
$K^{\star}_{\mathrm{cl}}\!=\!0$ everywhere. The gap is not an SPSA
failure or a meta-parameter mis-tuning; it is structural. Innovation
gives the agent information about \emph{sensor quality}, not about
\emph{whether the forward model is good enough to operate without
sensory help}. The second piece of information --- whether the
controller's planned trajectory is being followed --- is in the task
outcome, not in the innovation. C2 then shows that adding the
outcome layer through SPSA recovers the closed-loop oracle level
within $1$\,cm on a single cell. Together they show that, at least
on this task, a two-layer agent-available adaptation rule is
sufficient. Open question: whether reliability + outcome can also
be combined within a single trial (e.g.\ an outcome-modulated
$\beta$ during reaching) rather than only across trials.

\smallskip

\noindent\textbf{Single-$\beta$ adaptation has a structural ceiling
under cell heterogeneity.} The C3 result --- multi-cell SPSA closes
only $3$--$5$\,cm of the $\sim\!29$\,cm gap at delay-$18$ cells, and
none at delay-$0$ cells --- is not an SPSA convergence issue. It is
geometric: under $H{=}8$ on ReachFixed, $K^{\star}_{\mathrm{cl}}$ is
$K{=}0$ at every cell, but the realised reliability $r_f(t)$ trace
differs across cells. A single global $\beta_{0,f}$ that pushes $K$
to nearly $0$ at the high-noise cell over-suppresses at the
low-noise cell and vice versa. The natural fix is a per-cell or
per-context $\beta$, which corresponds biologically to context-
dependent gating; we have not implemented it here. A
related theoretical question is whether $K^{\star}_{\mathrm{cl}}$
under reasonable controllers admits a low-dimensional shared
structure across cells; if it does, a context-dependent extension of
$\beta$ should suffice.

\smallskip

\noindent\textbf{Task transfer reveals that the right rule depends
on $K^{\star}_{\mathrm{cl}}$ geometry.} The C4 finding is
paradoxical at first glance: default within-trial reliability,
trained on nothing, beats $K{=}0$ on ReachRandom; but
\emph{outcome-trained} $\beta$ from ReachFixed loses to $K{=}0$ on
ReachRandom. The mechanism is that $K^{\star}_{\mathrm{cl}}$ on
ReachRandom is multimodal across cells ($\{0, 0.25, 1\}$,
Appendix~\ref{app:reach-random}), so the default reliability rule's
$K_f\!\approx\!0.6$--$0.7$ accidentally lands close to the per-cell
optima in the delay-$18$ rows. Pushing $K_f$ globally down (as the
ReachFixed-trained $\beta$ does) is correct on the source task but
wrong on the target. The biological reading is that an agent
trained on a uniform-optimum task and asked to perform a multimodal-
optimum variant should be expected to perform worse than an agent
with a more conservative prior --- a falsifiable prediction about
how the structure of the closed-loop optimum should appear in cross-
task transfer experiments.

\smallskip

\noindent\textbf{Oracle-supervised gain as diagnostic upper bound.}
Appendix~\ref{app:oracle-supervised} reports an oracle-supervised
correction-gain predictor that requires per-cell $K$-sweep labels.
The closed-loop-oracle variant outputs $K\!\approx\!0.02$ and
matches $K{=}0$ on the focused grid within $1.5$\,cm at delay-$18$
cells, which is the strongest non-agent-available baseline for the
correction-gain problem. The C2 single-cell SPSA result effectively
reaches this level using only signals the agent has access to ---
prediction errors during the reach and the reaching outcome
afterward. We frame the oracle-supervised predictor not as a
biological model but as the upper bound against which agent-
available adaptation should be measured. The fact that a
$10$-parameter SPSA on per-episode outcome closes the gap on a
single training cell suggests the upper bound is reachable by
plausible biological signals; whether it is reachable at the
population level (i.e.\ a single $\beta$ over many cells) appears to
depend on $K^{\star}_{\mathrm{cl}}$ structure as C3 shows.

\smallskip

\noindent\textbf{Limits and open questions.} (a)~Two-layer rule only:
$\beta$ is the only learned object inside the reliability-adaptive
observer; the forward model is fixed. A jointly-trained forward
model + $\beta$ pair is a natural extension. (b)~Single-$\beta$
SPSA: the multi-cell C3 result is a ceiling of single-global-$\beta$
adaptation; a context-dependent $\beta$ network is the obvious next
variant and is left to future work. (c)~Two MyoSuite reach variants
(ReachFixed and ReachRandom), 34-muscle arm only. Transfer to
contact-rich tasks (grasp/place) and to other MyoSuite arms
(Elbow/Finger reach) remains open. (d)~Forward-model family: the
residual MLP is a coarse instantiation of the cerebellar forward-
model box; continuous-time recurrent alternatives such as
LTC~\cite{hasani2021liquid} and CfC~\cite{hasani2022cfc} would
provide a closer computational analogue when this observer is
plugged into a full cortico-cerebellar loop. (e)~The behaviour-
cloned controller (Appendix~\ref{app:oracle-supervised}) washes out
estimator differences at the demonstration scale we test;
closed-loop state-estimation benchmarks should keep this in mind
when choosing the downstream policy.

% ============================================================
\section{Conclusion}
\label{sec:conclusion}
% ============================================================

We have shown on a muscle-driven reaching task that a forward-model-
based predictive state observer can adapt its sensory prediction-
error correction gain from signals the agent itself has access to
--- within-trial innovation reliability and across-trial reaching
outcome --- without an offline oracle on the optimal gain. A two-
layer adaptation rule combining a logistic
$K_f(t)\!=\!\sigma(\beta_{0,f}\!+\!\beta_{1,f}\log r_f(t))$ over
field-wise reliability with SPSA across-trial updates of $\beta$
recovers the closed-loop oracle within $1$\,cm on a single training
cell, with no oracle access (C2). The same rule has two structural
limitations: when the closed-loop optimum is uniform across noise-
delay cells, a single global $\beta$ trained on a multi-cell mixture
closes only $3$--$5$\,cm of the gap (C3, cell-heterogeneity ceiling);
and a $\beta$ trained on one task does not transfer to a task whose
closed-loop optimum is multimodal (C4, task-dependent rule). We
read these as direct evidence that the correction-gain problem is
solvable from agent-available signals but that the resulting rule
must be context-dependent, not a single global $\beta$. Within the
forward-model + predictive-state-observer framing this is a model
of how a biological motor system might assemble its correction-gain
rule from prediction errors and reaching outcomes without ever
seeing the oracle.

% ============================================================
\section*{Acknowledgement}
% ============================================================
\noindent The author thanks the MyoSuite maintainers for releasing the
musculoskeletal benchmark used in this study.

\bibliographystyle{IEEEtran}
\bibliography{refs}

% ============================================================
\appendices
% ============================================================
\section{Hyperparameters and Implementation Details}
\label{app:hyperparams}

Table~\ref{tab:hyperparams} lists every hyperparameter used in the
paper. All training runs are reproducible from
\texttt{configs/} in the source repository; estimator evaluation grids
are listed verbatim in \texttt{configs/estimators/} and
\texttt{configs/closed\_loop/}.

\begin{table}[!htbp]
\centering
\caption{Hyperparameters used across the paper.}
\label{tab:hyperparams}
\renewcommand{\arraystretch}{1.15}
\setlength{\tabcolsep}{3pt}
\scriptsize
\resizebox{\linewidth}{!}{%
\begin{tabular}{@{}lll@{}}
\toprule
\textbf{Component} & \textbf{Hyperparameter} & \textbf{Value} \\
\midrule
\multirow{6}{*}{Forward model $f_\theta$}
  & state dim & 83 \\
  & action dim & 34 \\
  & hidden layers & 2 $\times$ 256 \\
  & activation & ReLU, LayerNorm \\
  & output & residual ($\Delta x$) \\
  & parameters & 118{,}355 \\
\midrule
\multirow{6}{*}{Forward training}
  & optimiser & Adam~\cite{kingma2015adam} \\
  & learning rate & $1\,{\times}\,10^{-3}$ \\
  & weight decay & 0 \\
  & batch size & 256 \\
  & epochs & 200 (early stop patience 20) \\
  & val split & every 5th source episode \\
\midrule
Multi-step loss
  & rollout horizon $H$ & 1, 4, 8 \\
\midrule
\multirow{4}{*}{Stage A $g_\phi$}
  & input dim & 8 = 3 ctrl + 4 $\sigma$ + 1 delay/$d_{\max}$ \\
  & hidden layers & 2 $\times$ 32 \\
  & output & sigmoid scalar in $[0,1]$ \\
  & parameters & 1{,}409 \\
\midrule
\multirow{4}{*}{Stage A training}
  & optimiser & Adam \\
  & learning rate & $1\,{\times}\,10^{-3}$ \\
  & weight decay & $1\,{\times}\,10^{-3}$ \\
  & epochs & 500 \\
\midrule
\multirow{3}{*}{Joint-PD controller}
  & $K_p$, $K_d$ & 30, 3 \\
  & action scale $\alpha$ & 5 \\
  & muscle map & $\mathrm{clip}(\alpha\,\mathrm{ReLU}(M\,u_{\mathrm{joint}}))$, $M{\in}\mathbb{R}^{34\times 20}$ \\
\midrule
\multirow{3}{*}{IK solver}
  & method & damped Jacobian pseudoinverse \\
  & max iter / tol & 200 / 0.01\,m \\
  & damping $\lambda^2$ & 0.1 \\
\midrule
\multirow{4}{*}{BC policy}
  & state dim variants & 83 (full) / 80 / 77 \\
  & hidden layers & 2 $\times$ 256 \\
  & output & sigmoid 34-dim \\
  & demos / training & 30 ep.\ $\times$ 600 steps; 100 epochs \\
\midrule
\multirow{3}{*}{Eval grid (open-loop)}
  & noise levels & none, low, medium, high, vhigh, xhigh \\
  & delays (steps) & 0, 6, 18, 36 \\
  & $K$ values & 0.0, 0.25, 0.5, 0.75, 1.0 \\
\midrule
\multirow{2}{*}{Eval grid (closed-loop)}
  & noise levels (focused) & none, high, xhigh \\
  & delays (focused) & 0, 18 \\
\bottomrule
\end{tabular}%
}
\end{table}

\section{Forward-Model Rollout Metrics}
\label{app:fwd-rollout}

Table~\ref{tab:fwd_rollout} reports horizon-wise prediction error for
the three forward-model variants used in the paper. Multi-step rollout
supervision substantially reduces medium- and long-horizon error while
paying a small cost at $h{=}1$.

\begin{table}[!htbp]
\centering
\caption{Forward-model rollout MSE and tip prediction error on the
held-out validation episodes. Lower is better. All three models share
the same architecture and training set (51{,}534 transitions across
106 episodes); they differ only in the rollout horizon $H$.}
\label{tab:fwd_rollout}
\setlength{\tabcolsep}{4pt}
\footnotesize
\begin{tabular}{@{}l ccc ccc@{}}
\toprule
& \multicolumn{3}{c}{MSE} & \multicolumn{3}{c}{Tip err.\ (m)} \\
\cmidrule(lr){2-4}\cmidrule(lr){5-7}
Model & $h{=}1$ & $h{=}10$ & $h{=}50$ & $h{=}1$ & $h{=}10$ & $h{=}50$ \\
\midrule
$H{=}1$ (single-step) & 0.0055 & 0.0124 & 0.0524 & 0.0076 & 0.0355 & 0.1381 \\
$H{=}4$ multi-step    & 0.0053 & 0.0084 & 0.0164 & 0.0088 & 0.0247 & 0.0635 \\
$H{=}8$ multi-step    & 0.0055 & 0.0082 & 0.0114 & 0.0098 & 0.0215 & 0.0483 \\
\bottomrule
\end{tabular}
\end{table}

\section{Oracle-Supervised Correction-Gain Predictor (Diagnostic Upper Bound)}
\label{app:oracle-supervised}

The reliability-adaptive observer of Sec.\,\ref{sec:method-reliability}
sets $K_f(t)$ from agent-available signals only. As a non-agent-
available upper bound we also train an offline-oracle-supervised
correction-gain predictor and evaluate it on the same grid; the
single-cell SPSA result of C2 effectively matches this predictor on
its training cell.

\paragraph{Predictor architecture.}
A small MLP $g_\phi:\mathbb{R}^{8}\!\to\![0,1]$ predicts the scalar
correction gain from a condition feature vector. The input
\begin{equation}
\xi = \bigl[\,
        \mathrm{onehot}(c)\,;\;
        \sigma\,;\;
        d/d_{\max}
      \,\bigr]
      \in \mathbb{R}^{8},
\label{eq:predictor_input_appendix}
\end{equation}
concatenates a 3-way controller one-hot $\mathrm{onehot}(c)$, the
4-dim noise-sigma vector $\sigma$ (\texttt{qpos}, \texttt{qvel},
\texttt{tip\_pos}, \texttt{reach\_err}), and a normalised delay
$d/d_{\max}$ with $d_{\max}{=}36$ steps. The network is a
$8\!\to\!32\!\to\!32\!\to\!1$ MLP with ReLU and a sigmoid output.

\paragraph{Oracles.}
For each of the 72 conditions in the stress grid ($3$~controllers
$\times\,6$~noise levels $\times\,4$~delays) we sweep
$K\!\in\!K_{\mathrm{grid}}\!=\!\{0, 0.25, 0.5, 0.75, 1.0\}$ and
select the $K$ minimising a chosen criterion. We define an
\emph{open-loop} oracle (estimation accuracy) and a \emph{closed-
loop} oracle (downstream task error):
\begin{align}
K^{\star}_{\mathrm{ol}}(\sigma, d, c)
  &= \arg\min_{K \in K_{\mathrm{grid}}}\;
     \mathbb{E}_t\!\big[\|\hat x_t(K) - x_t\|^2 \,\big|\, \sigma, d, c\big],
     \label{eq:K_ol}\\
K^{\star}_{\mathrm{cl}}(\sigma, d, c)
  &= \arg\min_{K \in K_{\mathrm{grid}}}\;
     \mathbb{E}\!\big[\mathrm{minTip}(K) \,\big|\, \sigma, d, c\big],
     \label{eq:K_cl}
\end{align}
where $\mathrm{minTip}$ is the closed-loop minimum tip-to-target
distance over the episode. Two predictor variants $g_\phi^{\mathrm{ol}}$
and $g_\phi^{\mathrm{cl}}$ are trained against
$K^{\star}_{\mathrm{ol}}$ and $K^{\star}_{\mathrm{cl}}$ respectively.

\paragraph{Closed-loop deployment.}
Under the $H{=}8$ forward model on the focused 6-cell joint-PD grid,
$g_\phi^{\mathrm{ol}}$ outputs $K\!\approx\!0.36$--$0.99$ and trails
$K{=}0$ by $14$--$28$\,cm (the open-loop oracle never selects $K{=}0$
because the open-loop estimation error is minimised by trusting the
observation when the buffer rollout is long). The closed-loop sweep
of Appendix~\ref{app:cl-sweep} fixes this: $K^{\star}_{\mathrm{cl}}
\!=\!0$ at every cell, the retrained $g_\phi^{\mathrm{cl}}$ outputs
$K\!\approx\!0.02$, and the predictor matches $K{=}0$ within
$1.5$\,cm at the three delay-$18$ cells. A residual $\sim 17$\,cm gap
persists at the three delay-$0$ cells because the closed-loop
$K$-curve jumps sharply from $K{=}0$ to $K{=}0.25$ there
(Appendix~\ref{app:cl-sweep}) and the sigmoid asymptote $0.02$ lies
on the wrong side of the jump. Across the six cells,
$g_\phi^{\mathrm{cl}}$ closes $40$--$60\%$ of the
$g_\phi^{\mathrm{ol}}$-vs-$K{=}0$ gap with no architectural change to
the predictor.

\paragraph{Transfer to the endpoint-error sensorimotor policy.}
Repeating the closed-loop deployment with controller (iii) (endpoint-
error sensorimotor policy, Sec.\,\ref{sec:method}, item iii) shows
the same qualitative pattern, in a weaker form: $K{=}0$ beats $K{=}1$
by $-18.5$ to $-18.7$\,cm at the three $d{=}0$ cells and the two are
within $\pm 3.2$\,cm at $d{=}18$ (Table~\ref{tab:endpoint_feedback}).
The endpoint-error policy's $K{=}0$ baseline is $\sim\!11$\,cm worse
in absolute terms than joint-PD's because the Jacobian-transpose
projection is locally correct only; the qualitative pattern
(closed-loop $K^{\star}\!=\!0$ on this grid) is preserved.

\begin{table}[!htbp]
\centering
\caption{Oracle-supervised predictor deployed on the endpoint-error
sensorimotor policy (iii) on the focused 6-cell grid under the
$H{=}8$ forward model. Mean $\min$-tip (m) over 10 episodes per
cell. The ``learned-cl'' column is $g_\phi^{\mathrm{cl}}$ trained on
joint-PD $K^{\star}_{\mathrm{cl}}$ labels.}
\label{tab:endpoint_feedback}
\setlength{\tabcolsep}{6pt}
\footnotesize
\begin{tabular}{@{}ll ccc@{}}
\toprule
noise & delay & $K{=}0$ & $K{=}1$ & learned-cl ($\bar K\!\approx\!0.02$) \\
\midrule
none  & 0  & 0.604 & 0.789 & 0.754 \\
none  & 18 & 0.647 & 0.631 & 0.687 \\
high  & 0  & 0.604 & 0.789 & 0.693 \\
high  & 18 & 0.647 & 0.614 & 0.652 \\
xhigh & 0  & 0.604 & 0.791 & 0.656 \\
xhigh & 18 & 0.647 & 0.628 & 0.628 \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Behaviour-cloned policy washes out the signal.}
The behaviour-cloned controller (iv) (a two-hidden-layer 256-unit
sigmoid MLP trained on 30 scripted IK demonstrations) reaches better
than the joint-PD baseline in absolute terms ($\mathrm{success}_{<0.10\,
\mathrm{m}}$ from $\sim 12\%$ to $\sim 30\%$ in the easier cells) but
the gap between any two estimators collapses to under $3$\,cm. We
take this as evidence that the BC policy at this demonstration scale
is effectively open-loop in the state channel and report it as a
methodological caution for closed-loop estimator benchmarks: the
downstream controller must be demonstrably state-coupled, or any
observer differences will not surface. The behaviour-cloned
controller is therefore retained only as a diagnostic and is not
used in the main C1--C4 results.

\paragraph{Why we keep this as an upper-bound baseline only.}
$g_\phi^{\mathrm{cl}}$ requires a per-condition $K$-sweep that the
biological agent does not have access to. We report it as the
strongest baseline that uses non-agent-available information; the
C2 single-cell SPSA result matches it on the training cell while
using only innovation history and per-episode outcome, both of
which the agent can plausibly compute on-line.

\section{Full Open-Loop Oracle $K^{\star}$ Grids}
\label{app:oracle-grids}

Table~\ref{tab:oracle_K8} reports the mean oracle $K^{\star}$ per
(delay, noise) cell under the $H{=}8$ multi-step-supervised forward
model used throughout the main results, supervising the open-loop
predictor variant $g_\phi^{\mathrm{ol}}$ of
Appendix~\ref{app:oracle-supervised}. Under single-step supervision
($H{=}1$) the $K^{\star}{<}1$ regime is confined to zero delay (18 of
72 cells); $H{=}4$ supervision widens the regime to 26 of 72 cells.
The qualitative pattern (column-wise decrease at $d{=}0$,
column-wise increase with delay) is the same across all three
forward-model variants.

\begin{table}[!htbp]
\centering
\caption{Oracle correction gain $K^{\star}$ as a function of delay
and noise under the $H{=}8$ multi-step-supervised forward model
(mean over 3 controllers; 72 conditions total). 27 of 72 cells
require $K^{\star}{<}1$.}
\label{tab:oracle_K8}
\footnotesize
\begin{tabular}{@{}c ccc ccc@{}}
\toprule
& \multicolumn{6}{c}{noise} \\
\cmidrule(l){2-7}
delay & none & low & medium & high & vhigh & xhigh \\
\midrule
0      & 1.000 & 0.833 & 0.667 & 0.417 & 0.333 & 0.250 \\
6      & 1.000 & 1.000 & 1.000 & 0.833 & 0.750 & 0.500 \\
18     & 1.000 & 1.000 & 1.000 & 0.917 & 0.917 & 0.833 \\
36     & 1.000 & 1.000 & 1.000 & 1.000 & 1.000 & 0.833 \\
\bottomrule
\end{tabular}
\end{table}

\section{Closed-Loop $K$-Sweep on the Focused Grid}
\label{app:cl-sweep}

Table~\ref{tab:closed_K_sweep} reports the closed-loop $\min$-tip-to-
target error per cell as a function of the (fixed) correction gain
$K$, under the $H{=}8$ forward model and the joint-PD controller
(10 episodes per cell). This $K$-sweep grounds two arguments in the
main text: it defines the closed-loop oracle
$K^{\star}_{\mathrm{cl}}$ used by the diagnostic predictor of
Appendix~\ref{app:oracle-supervised}, and it is the baseline against
which the reliability-adaptive observer is compared in
Sec.\,\ref{sec:results-c1}--\ref{sec:results-c2}.
$K^{\star}_{\mathrm{cl}}{=}0$ in every cell of the focused grid.

Two regularities are worth noting. First, at zero delay the K-curve
is sharp around $K{=}0$: moving from $K{=}0$ to $K{=}0.25$ jumps
$\min$-tip from 0.49\,m to 0.77\,m. The oracle-supervised predictor's
sigmoid asymptote ($K\!\approx\!0.025$,
Appendix~\ref{app:oracle-supervised}) sits on the wrong side of this
jump in delay-0 cells, which is why the diagnostic gap remains
($\sim\!17$\,cm). Second, at delay-18 the K-curve is smooth near
$K{=}0$ ($K{=}0$ and $K{=}0.25$ within 0.01--0.05\,m) so the
predictor's small offset is harmless and the same predictor matches
the $K{=}0$ baseline within 1.5\,cm. These regularities also explain
why the reliability-adaptive observer of C1 (which floats around
$K_f\!\approx\!0.6$--$0.7$) does badly at every delay-18 cell and at
all delay-0 cells alike: any non-trivial $K$ lands in the high-error
plateau.

\begin{table}[!htbp]
\centering
\caption{Closed-loop $\min$-tip error (m) per cell across the
$K$-sweep (correction gain) under the $H{=}8$ forward model and the
joint-PD controller. Each cell averages 10 episodes. Bold marks the
per-cell minimum.}
\label{tab:closed_K_sweep}
\footnotesize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{@{}ll ccccc@{}}
\toprule
noise & delay & $K{=}0$ & $K{=}0.25$ & $K{=}0.5$ & $K{=}0.75$ & $K{=}1$ \\
\midrule
none  & 0  & \textbf{0.493} & 0.771 & 0.782 & 0.774 & 0.770 \\
none  & 18 & \textbf{0.497} & 0.498 & 0.708 & 0.746 & 0.697 \\
high  & 0  & \textbf{0.493} & 0.767 & 0.780 & 0.771 & 0.770 \\
high  & 18 & \textbf{0.497} & 0.538 & 0.696 & 0.746 & 0.691 \\
xhigh & 0  & \textbf{0.493} & 0.772 & 0.769 & 0.766 & 0.762 \\
xhigh & 18 & \textbf{0.497} & 0.550 & 0.713 & 0.749 & 0.700 \\
\bottomrule
\end{tabular}
\end{table}

\section{Transfer Test on ReachRandom}
\label{app:reach-random}

\noindent\textbf{Setup.} We re-run a slim version of the pipeline on
\texttt{myoArmReachRandom-v0} (same 34-muscle arm, same 600-step
episodes, random per-episode target instead of a fixed one). Targets
come from the same \texttt{runs/targets/train.npz} file (200
positions) so that targets are paired across estimators and forward
models; initial joint pose is deterministic ($q{=}0$) for both
variants. Transition data: 49{,}740 transitions across 106 episodes
(random / lowamp / hold controllers), comparable to the 51{,}534-
transition ReachFixed dataset. The marginal C2 cell-level effect
($\approx 8.6$\,cm at one cell with learned $K\!\approx\!0.99$ and
paired-$t$ $p{=}0.21$) is task-dependent and \emph{not} treated as a
transfer target; the test asks whether the structural phenomena C1 /
C3 / C4 persist under variable-target reaching. Pre-committed pass
criteria, fixed before reading the numbers:
\begin{itemize}
\item \emph{C1 replicate}: open-loop oracle $K^{\star}_{\mathrm{ol}}$
varies with noise / delay, with long-delay cells biasing toward
$K{=}1$.
\item \emph{C3 replicate}: multi-step supervision reduces $h{=}50$
forward-model error and increases the share of closed-loop cells
where $K{=}0$ or prediction-heavy estimators beat $K{=}1$.
\item \emph{C4 replicate}: a closed-loop oracle
$K^{\star}_{\mathrm{cl}}$ trained predictor moves toward the K=0 /
$K^{\star}_{\mathrm{cl}}$ side of the K-curve compared with an
open-loop oracle predictor.
\end{itemize}

\noindent\textbf{Forward-model rollout error} (Table~\ref{tab:rr_fm})
mirrors the ReachFixed pattern: $H{=}8$ multi-step rollout supervision
reduces $h{=}50$ tip prediction error by $48\%$ versus $H{=}1$
($0.110\to0.057$\,m).

\begin{table}[!htbp]
\centering
\caption{ReachRandom forward-model rollout MSE and tip prediction
error on held-out validation episodes. Same network and training
recipe as the main text; lower is better.}
\label{tab:rr_fm}
\setlength{\tabcolsep}{4pt}
\footnotesize
\begin{tabular}{@{}l ccc ccc@{}}
\toprule
& \multicolumn{3}{c}{MSE} & \multicolumn{3}{c}{Tip err.\ (m)} \\
\cmidrule(lr){2-4}\cmidrule(lr){5-7}
Model & $h{=}1$ & $h{=}10$ & $h{=}50$ & $h{=}1$ & $h{=}10$ & $h{=}50$ \\
\midrule
$H{=}1$ (single-step) & 0.0045 & 0.0103 & 0.0318 & 0.0066 & 0.0291 & 0.1098 \\
$H{=}8$ multi-step    & 0.0046 & 0.0072 & 0.0096 & 0.0093 & 0.0229 & 0.0573 \\
\bottomrule
\end{tabular}
\end{table}

\noindent\textbf{Closed-loop reaching, single-step model.}
Table~\ref{tab:rr_h1_baseline} reports $\min$-tip error at the 6-cell
focused grid for $K{=}0$ and $K{=}1$ under the joint-PD controller.
Two observations: (i) $K{=}0$ is constant across noise/delay because
it ignores observations, and (ii) on ReachRandom $K{=}0$ already
beats $K{=}1$ in \emph{every} cell, by 7--27\,cm. This contrasts with
ReachFixed where the $H{=}1$ closed-loop optimum was nearer $K{=}1$
at delay-18 cells; the gap is therefore task-dependent at the
single-step baseline.

\begin{table}[!htbp]
\centering
\caption{ReachRandom $H{=}1$ closed-loop $\min$-tip error (m, mean
over 10 episodes) under the joint-PD controller. $K{=}0$ beats
$K{=}1$ in every cell by 7--27\,cm.}
\label{tab:rr_h1_baseline}
\footnotesize
\setlength{\tabcolsep}{6pt}
\begin{tabular}{@{}ll cc r@{}}
\toprule
noise & delay & $K{=}0$ & $K{=}1$ & $K{=}1{-}K{=}0$ \\
\midrule
none  & 0  & 0.499 & 0.770 & $+27.2$\,cm \\
none  & 18 & 0.498 & 0.578 & $+8.0$\,cm  \\
high  & 0  & 0.499 & 0.770 & $+27.2$\,cm \\
high  & 18 & 0.498 & 0.567 & $+6.8$\,cm  \\
xhigh & 0  & 0.499 & 0.762 & $+26.3$\,cm \\
xhigh & 18 & 0.498 & 0.593 & $+9.4$\,cm  \\
\bottomrule
\end{tabular}
\end{table}

\noindent\textbf{Closed-loop K-curve, $H{=}8$ multi-step model.}
Table~\ref{tab:rr_h8_ksweep} reports the full
$K\!\in\!\{0,0.25,0.5,0.75,1\}$ sweep at $H{=}8$. The closed-loop
oracle $K^{\star}_{\mathrm{cl}}$ is the per-cell minimiser. Unlike
ReachFixed, where $K^{\star}_{\mathrm{cl}}{=}0$ in every cell of the
focused grid, on ReachRandom $K^{\star}_{\mathrm{cl}}$ splits across
three values: $K{=}0$ at all three $d{=}0$ cells (matching
ReachFixed), $K{=}0.25$ at delay-18 in the low/medium-noise cells,
and $K{=}1$ at the (xhigh, $d{=}18$) cell.

\begin{table}[!htbp]
\centering
\caption{ReachRandom $H{=}8$ closed-loop $\min$-tip K-sweep (mean over
10 episodes). Bold marks the per-cell $K^{\star}_{\mathrm{cl}}$.}
\label{tab:rr_h8_ksweep}
\setlength{\tabcolsep}{4pt}
\footnotesize
\begin{tabular}{@{}ll ccccc@{}}
\toprule
noise & delay & $K{=}0$ & $K{=}0.25$ & $K{=}0.5$ & $K{=}0.75$ & $K{=}1$ \\
\midrule
none  & 0  & \textbf{0.670} & 0.762 & 0.785 & 0.780 & 0.770 \\
none  & 18 & 0.642 & \textbf{0.625} & 0.706 & 0.644 & 0.647 \\
high  & 0  & \textbf{0.670} & 0.764 & 0.785 & 0.775 & 0.770 \\
high  & 18 & 0.642 & \textbf{0.631} & 0.688 & 0.651 & 0.639 \\
xhigh & 0  & \textbf{0.670} & 0.758 & 0.765 & 0.761 & 0.762 \\
xhigh & 18 & 0.642 & 0.662 & 0.691 & 0.650 & \textbf{0.627} \\
\bottomrule
\end{tabular}
\end{table}

\noindent\textbf{Open-loop oracle structure on ReachRandom.} We re-ran
the full $3\,\text{controllers} \times 6\,\text{noise} \times
4\,\text{delay} \times 5\,K$ stress eval (using 2 episodes per
controller, matching the ReachFixed scale) for both forward models.
$K^{\star}_{\mathrm{ol}} < 1$ holds in 18 / 72 cells under $H{=}1$
(exactly matching ReachFixed at $H{=}1$) and 25 / 72 cells under
$H{=}8$ (vs.\ 27 / 72 on ReachFixed); blending is again confined to
delay-0 cells under $H{=}1$ and expands to delay $\geq 6$ under $H{=}8$
(Table~\ref{tab:rr_oracle}). C1 therefore replicates structurally on
ReachRandom.

\begin{table}[!htbp]
\centering
\caption{ReachRandom $K^{\star}_{\mathrm{ol}}$ (mean over 3 controllers)
per (noise, delay) cell, under $H{=}1$ (top) and $H{=}8$ (bottom).
Dashes mark $K^{\star}_{\mathrm{ol}}{=}1.0$ (trust the observation).
The non-trivial $K^{\star}_{\mathrm{ol}} < 1$ regime is confined to
delay-0 under $H{=}1$ and spreads to delay $\geq 6$ under $H{=}8$,
mirroring ReachFixed.}
\label{tab:rr_oracle}
\setlength{\tabcolsep}{4pt}
\footnotesize
\begin{tabular}{@{}l cccc@{}}
\toprule
\multicolumn{5}{@{}l}{$H{=}1$ single-step forward model} \\
\midrule
noise $\backslash$ delay & 0 & 6 & 18 & 36 \\
\midrule
none   & --    & --    & -- & -- \\
low    & 0.83 & --    & -- & -- \\
medium & 0.67 & --    & -- & -- \\
high   & 0.50 & --    & -- & -- \\
vhigh  & 0.33 & 0.92 & -- & -- \\
xhigh  & 0.25 & 0.67 & -- & -- \\
\midrule
\multicolumn{5}{@{}l}{$H{=}8$ multi-step forward model} \\
\midrule
noise $\backslash$ delay & 0 & 6 & 18 & 36 \\
\midrule
none   & --    & --    & --    & -- \\
low    & 0.83 & --    & --    & -- \\
medium & 0.67 & --    & --    & -- \\
high   & 0.42 & 0.92 & --    & -- \\
vhigh  & 0.33 & 0.67 & 0.92 & -- \\
xhigh  & 0.25 & 0.50 & 0.75 & 0.75 \\
\bottomrule
\end{tabular}
\end{table}

\noindent\textbf{Learned predictors trained on
$K^{\star}_{\mathrm{ol}}$ vs $K^{\star}_{\mathrm{cl}}$.} We train the
same $8{\to}32{\to}32{\to}1$ Stage A predictor on (i) the
$K^{\star}_{\mathrm{ol}}$ table from the $H{=}8$ ReachRandom stress
eval, and (ii) the per-cell $K^{\star}_{\mathrm{cl}}$ labels from
Table~\ref{tab:rr_h8_ksweep} (extended to the 72-cell schema by
replicating the joint-PD label across the auxiliary controllers and
the un-swept delays/noises). Table~\ref{tab:rr_learned} reports the
$K$ each predictor outputs at the focused grid and the resulting
closed-loop $\min$-tip. The $K^{\star}_{\mathrm{ol}}$ predictor
recovers values near $K{=}1$ at delay-18 cells (its training labels
were $K^{\star}_{\mathrm{ol}}{=}1$ there), matching the
$K{=}1$ baseline within $1$\,cm. The $K^{\star}_{\mathrm{cl}}$
predictor recovers small $K$ ($0.06$--$0.31$) at delay-0 cells,
beating the $K^{\star}_{\mathrm{ol}}$ predictor by $1.3$--$12.6$\,cm
there. At delay-18 the $K^{\star}_{\mathrm{cl}}$ predictor's outputs
drift toward intermediate values ($K{=}0.24$--$0.95$) and the resulting
closed-loop reach is comparable to $K{=}1$ (within $\pm 8$\,cm). The
oracle-redefinition transfer therefore holds clearly at delay-0 but
is muted at delay-18 because the closed-loop K-curve there is
relatively flat (Table~\ref{tab:rr_h8_ksweep}) and the sigmoid
output cannot cleanly land on the mixed $K^{\star}_{\mathrm{cl}}$
($0/0.25/1$) values across the three noise rows.

\begin{table}[!htbp]
\centering
\caption{ReachRandom $H{=}8$ closed-loop comparison of four
estimators on the focused grid. Columns: $K{=}0$, $K{=}1$,
\emph{learned-ol} (Stage A trained on
$K^{\star}_{\mathrm{ol}}$), \emph{learned-cl} (Stage A trained on
$K^{\star}_{\mathrm{cl}}$); for each learned predictor we report
both its mean output $\bar K$ and the closed-loop $\min$-tip (m,
mean over 10 episodes).}
\label{tab:rr_learned}
\setlength{\tabcolsep}{3pt}
\footnotesize
\begin{tabular}{@{}ll cc cc cc@{}}
\toprule
\multirow{2}{*}{noise} & \multirow{2}{*}{delay}
  & \multirow{2}{*}{$K{=}0$} & \multirow{2}{*}{$K{=}1$}
  & \multicolumn{2}{c}{learned-ol} & \multicolumn{2}{c}{learned-cl} \\
\cmidrule(lr){5-6}\cmidrule(lr){7-8}
& & & & $\bar K$ & m & $\bar K$ & m \\
\midrule
none  & 0  & 0.670 & 0.770 & 0.80 & 0.781 & 0.06 & 0.655 \\
none  & 18 & 0.642 & 0.647 & 0.98 & 0.638 & 0.24 & 0.673 \\
high  & 0  & 0.670 & 0.770 & 0.71 & 0.777 & 0.09 & 0.690 \\
high  & 18 & 0.642 & 0.639 & 0.96 & 0.644 & 0.47 & 0.721 \\
xhigh & 0  & 0.670 & 0.762 & 0.36 & 0.783 & 0.31 & 0.770 \\
xhigh & 18 & 0.642 & 0.627 & 0.88 & 0.654 & 0.95 & 0.642 \\
\bottomrule
\end{tabular}
\end{table}

\noindent\textbf{Assessment against the pre-committed criteria.}
\begin{itemize}
\item \emph{C1 (full replication)}: open-loop oracle
$K^{\star}_{\mathrm{ol}}$ varies with noise and delay in the same
qualitative shape as ReachFixed, with the $K^{\star}_{\mathrm{ol}}<1$
fraction growing from 18 / 72 ($H{=}1$) to 25 / 72 ($H{=}8$).
\item \emph{C3 (partial replication)}: $H{=}8$ rollout supervision
reduces forward-model error by 48\,\% (replicates) and $K{=}0$ is
optimal at all three delay-0 cells (replicates). The
ReachFixed-style \emph{regime shift} from $K{=}1$-winning at $H{=}1$
to $K{=}0$-winning at $H{=}8$ does \emph{not} apply because $K{=}0$
was already winning at $H{=}1$; the variable-target variant is
structurally more favourable to prediction-only across forward-model
strengths.
\item \emph{C4 (partial replication, made richer)}: the closed-loop
oracle $K^{\star}_{\mathrm{cl}}$ on ReachRandom is \emph{not}
uniformly $K{=}0$ (3 cells pick $K{=}0$, 2 cells pick $K{=}0.25$,
1 cell picks $K{=}1$), confirming $K^{\star}_{\mathrm{cl}}$ is a
non-degenerate task-and-cell-dependent function. A Stage A predictor
retrained on these labels matches the $K{=}0$ baseline within
$1.3$\,cm at every delay-0 cell and beats the
$K^{\star}_{\mathrm{ol}}$-trained predictor by 1--13\,cm there. At
delay-18, where $K^{\star}_{\mathrm{cl}}$ takes three different values
across the three noise rows, the predictor's sigmoid output cannot
cleanly land on each label, and the closed-loop benefit is muted.
\end{itemize}

\noindent\textbf{Take-away.} On ReachRandom the closed-loop oracle
$K^{\star}_{\mathrm{cl}}$ is multimodal across cells
($\{0,0.25,1\}$), in contrast to the uniform $K^{\star}_{\mathrm{cl}}
\!=\!0$ on ReachFixed. The diagnostic oracle-supervised predictor
$g_\phi^{\mathrm{cl}}$ of Appendix~\ref{app:oracle-supervised}
transfers cleanly to delay-$0$ cells; at delay-$18$ the sigmoid-
output parameterisation cannot fully exploit the cell-dependent
$K^{\star}_{\mathrm{cl}}$. The same heterogeneity drives the C4
finding of Sec.\,\ref{sec:results-c4}: \emph{default within-trial
reliability} (no SPSA training of $\beta$) beats $K{=}0$ at
delay-$18$ cells on ReachRandom because its $K_f\!\approx\!0.6$--
$0.7$ accidentally lands near the per-cell oracle values, while the
ReachFixed-trained $\beta$ pushes $K_f$ globally lower and worsens
performance on ReachRandom.

\section{Software, Hardware, and Reproducibility}
\label{app:repro}

All experiments run on a single workstation (no GPU is required;
the forward model and Stage A predictor are small enough that CPU
training takes only minutes). Software stack: Python 3.11,
PyTorch (CPU), MuJoCo 2.3, MyoSuite~2.12.2, Gymnasium 0.29.
Reproduction scripts and exact dataset paths are recorded in the
source repository's \texttt{docs/02\_InitialImplementationPlan.md}
and \texttt{docs/03\_PaperOutline.md}.

\end{document}

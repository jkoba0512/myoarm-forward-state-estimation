% myoArm Forward State Estimation -- TNNLS draft
%
% Build:
%   cd paper && pdflatex main && bibtex main && pdflatex main && pdflatex main
%
% TNNLS uses the standard IEEEtran.cls (journal mode). Authors / affiliations
% are intentionally left blank for the v0 draft.

\documentclass[journal]{IEEEtran}

\usepackage{cite}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{algorithmic}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{textcomp}
\usepackage{xcolor}
\usepackage{url}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,calc,fit,backgrounds}
\usepackage{hyperref}
\hypersetup{colorlinks=true,linkcolor=blue,citecolor=blue,urlcolor=blue}

\graphicspath{{../figures/}}

% Convenience macros for the experiment naming used in the running text.
\newcommand{\K}[1]{$K{=}#1$}
\newcommand{\Kvar}{$K^{\star}$}
\newcommand{\mtip}{\,\text{m}}
\newcommand{\mTip}{m}
\newcommand{\hmax}[1]{$h{=}#1$}

\begin{document}

\title{When Should a Kalman Filter Trust Its Forward Model?\\
Closed-Loop Evidence from a Muscle-Driven Reaching Task}

\author{
    Anonymous Submission% Authors filled in at submission
    \thanks{Manuscript draft for IEEE Transactions on Neural Networks and
    Learning Systems. This work was supported in part by [redacted].
    Corresponding author: [redacted].}%
}

\markboth{IEEE Transactions on Neural Networks and Learning Systems, vol.~X,
no.~X, MMM~YYYY}%
{Anonymous \MakeLowercase{\textit{et al.}}: When Should a Kalman Filter Trust
Its Forward Model?}

\maketitle

\begin{abstract}
We study the role of a learned forward model and a learned Kalman gain in a
closed-loop muscle-driven reaching task. Using a 34-muscle MyoSuite arm and a
fixed-lag Kalman estimator that blends a residual-MLP forward prediction with
delayed noisy observations, we sweep observation noise and delay to define an
open-loop oracle Kalman gain $K^{\star}$ and train a small condition-level
predictor on those labels. In closed loop, a joint-space PD controller with
inverse-kinematics target reveals three findings. (i)~With a single-step-
supervised forward model, the learned predictor beats observation-only by
$8.6$\,cm at a single low-noise large-delay cell. (ii)~When the forward
model is retrained with $K$-step rollout supervision ($K\!\in\!\{4,8\}$),
its long-horizon tip-prediction error drops by 54--65\% and the closed-loop
optimum flips: \emph{prediction-only} ($K{=}0$) becomes the best estimator
across the entire grid, beating observation-only by 12--24\,cm, while the
condition-level predictor still outputs $K\!\approx\!1$ because it is trained
on the open-loop oracle. (iii)~Redefining the predictor's training oracle as
closed-loop task error rather than open-loop estimation accuracy (Phase~C)
moves its output to $K\!\approx\!0.02$, recovers the $K{=}0$ baseline within
$1.5$\,cm at delay $\geq 18$\,steps, and closes 40--60\% of the gap at zero
delay --- with no change to the predictor architecture. The results show
that the right Kalman gain in closed loop depends not only on the noise/delay
regime but on \emph{what objective the predictor was trained against}, and
that learned-Kalman-gain papers should specify this oracle choice explicitly.
\end{abstract}

\begin{IEEEkeywords}
Forward model, Kalman filter, state estimation, multi-step rollout
supervision, behaviour cloning, closed-loop control, MyoSuite, muscle-driven
reaching.
\end{IEEEkeywords}

% ============================================================
\section{Introduction}
% ============================================================

State estimation in muscle-driven manipulation faces three coupled challenges:
sensorimotor delay, observation noise, and signal-dependent motor noise. A
common engineering response is the Kalman filter, which blends a forward
model's prediction with the current observation via a gain $K\!\in\![0,1]$:
$K{=}0$ trusts the model, $K{=}1$ trusts the sensor, and the literature on
adaptive Kalman filters has produced many ways to choose $K$ from the
estimated noise covariance \cite{kalman1960,mehra1970,anderson1979optimal}.
The internal-model view of biological motor control casts the same
relationship as a forward-model / sensory-feedback combination
\cite{bernstein1967,wolpert2000}.
In this paper we ask a different question: \emph{what does the optimal $K$
look like in closed loop, and does the way we train a learned $K$-predictor
match it?}

We attack the question on a 34-muscle MyoSuite reaching arm
\cite{caggiano2022myosuite} running in the MuJoCo simulator
\cite{todorov2012mujoco}. We learn a residual-MLP forward model from rollouts,
use it inside a fixed-lag Kalman filter that handles a configurable
observation delay (\K{0}--$K{=}36$\,steps) and configurable per-field
Gaussian noise, sweep a $K{\in}\{0,0.25,0.5,0.75,1.0\}$ grid across noise
and delay to define an oracle gain $K^{\star}$ per condition, and train a
small condition-level predictor on those oracle labels. A separate
joint-space PD controller with one-shot inverse-kinematics target then
\emph{deploys} the estimator in closed loop.

Our contribution is five-fold (R1--R5/R6 below). The first three confirm and
quantify a textbook intuition; the last two are negative-then-positive
findings about how to train an adaptive Kalman gain.

\begin{itemize}
\item \textbf{R1.} Under a single-step-supervised forward model the oracle
$K^{\star}$ falls from $1.0$ to $0.25$ across noise at zero delay but
\emph{collapses uniformly to $1.0$ at delay $\geq 18$\,steps}, because the
forward-model rollout error compounds faster than observation noise grows
(Fig.\,\ref{fig:F2_oracle}, left panel).

\item \textbf{R3.} A learned condition-level predictor matches the oracle
within $0.6$\,cm on average and beats $K{=}1$ by \textbf{8.6\,cm} in
closed-loop reaching at the (low-noise, large-delay) cell where blending
matters most (Fig.\,\ref{fig:F4_phase2d}). Behaviour-cloned controllers
improve reaching but \emph{wash out} the estimator signal because the small
demonstration set causes the policy to collapse to an open-loop average
(Fig.\,\ref{fig:F6_tradeoff}).

\item \textbf{R5.} Retraining the forward model with $K$-step rollout
supervision ($K{=}4$ then $K{=}8$) reduces $h{=}50$ tip-prediction error by
54--65\% and \emph{widens} the open-loop K${<}1$ regime to delay
$\geq 18$\,steps. In closed loop, however, \emph{prediction-only}
($K{=}0$) becomes globally optimal: it beats $K{=}1$ by 12--24\,cm
in $\min$-tip error and the condition-level predictor still outputs
$K\!\approx\!1$ because the open-loop oracle it was trained on never
selects $K{=}0$ (Fig.\,\ref{fig:F7_paradigm}, panels 1--3).

\item \textbf{R6.} Redefining the predictor's training oracle as closed-loop
task error (per-cell K-sweep of closed-loop $\min$-tip) and retraining the
same MLP recovers $K\!\approx\!0.02$, matches the $K{=}0$ baseline within
$1.5$\,cm at all delay-18 cells, and closes 40--60\% of the gap at
delay-0 cells (Fig.\,\ref{fig:F7_paradigm}, panel~4). The residual
delay-0 gap is structural: the K-curve there jumps sharply from $K{=}0$
to $K{=}0.25$, so the predictor's sigmoid asymptote loses $\sim 0.17$\,m.
\end{itemize}

The combined narrative is that, in closed-loop control, the optimal Kalman
gain depends on (a)~the forward model's accuracy over the controller's
planning horizon and (b)~the oracle used to train any learned $K$-predictor;
the two regimes can fight each other when the open-loop oracle is used
without thought.

The rest of the paper is organised as follows. Section~\ref{sec:method}
describes the task, the forward model and multi-step supervision, the
fixed-lag Kalman estimator, the controllers, and the evaluation pipeline.
Section~\ref{sec:results} reports the results in order R1--R6 with the
corresponding figures. Section~\ref{sec:discussion} discusses the
trade-offs and implications for learned adaptive Kalman gains.

% ============================================================
\section{Method}
\label{sec:method}
% ============================================================

\subsection{Task and Environment}
We use the \texttt{myoArmReachFixed-v0} task in MyoSuite\,2.12.2, a 34-muscle
shoulder-elbow-wrist-hand system actuated by activations in $[0,1]^{34}$.
The task is to drive the index-finger tip to a randomly drawn target in a
$12$\,s episode at $\Delta t{=}0.02$\,s (600 control steps). The flat state
$x\!\in\!\mathbb{R}^{83}$ stacks $q$ (20), $\dot q$ (20), activation
(34), tip position (3), target (3), and reach error (3). True state is
extracted from MuJoCo and used only as an oracle for evaluation; the closed
loop never accesses it (Fig.\,\ref{fig:system}).

\begin{figure*}[t]
\centering
\begin{tikzpicture}[
    >={Stealth[length=2mm]},
    box/.style={draw, rounded corners=2pt, minimum height=10mm,
                align=center, font=\footnotesize, inner sep=3pt},
    env/.style={box, fill=gray!12, minimum width=22mm},
    obs/.style={box, fill=blue!10, minimum width=22mm},
    est/.style={box, fill=green!12, minimum width=34mm, minimum height=14mm},
    fwd/.style={box, fill=orange!18, minimum width=28mm, font=\scriptsize,
                minimum height=9mm},
    ctl/.style={box, fill=violet!15, minimum width=22mm},
    eval/.style={box, dashed, fill=red!8, minimum width=28mm,
                 font=\scriptsize\itshape},
    label/.style={font=\scriptsize, midway, fill=white, inner sep=1pt},
    node distance=6mm and 11mm,
]
% Main horizontal flow
\node[env] (env) {MuJoCo /\\MyoSuite};
\node[obs, right=of env] (obs) {Obs.\ wrappers\\(noise + delay)};
\node[est, right=of obs] (est) {Kalman estimator\\
        $\hat x_t {=} x_{\mathrm{pred}} {+} K(y_t {-} x_{\mathrm{pred}})$};
\node[ctl, right=of est] (ctl) {Controller\\(PD / BC)};

% Forward model below the estimator (slightly offset)
\node[fwd, below=10mm of est] (fwd) {Forward model $f_\theta$\\(residual MLP)};

% Evaluation-only box -- separate, below env
\node[eval, below=10mm of env] (eval) {True state $x_t$\\(oracle for eval)};

% Arrows on the main flow
\draw[->] (env) -- node[label, above]{$x_t$} (obs);
\draw[->] (obs) -- node[label, above]{$y_t$} (est);
\draw[->] (est) -- node[label, above]{$\hat x_t$} (ctl);

% Forward-model bidirectional connection with the estimator
\draw[<-] ($(est.south) + (-9mm,0)$) -- ($(fwd.north) + (-9mm,0)$)
   node[label, midway, fill=white, right=-0.5mm]{$x_{\mathrm{pred}}$};
\draw[->] ($(est.south) + (9mm,0)$) -- ($(fwd.north) + (9mm,0)$)
   node[label, midway, fill=white, right=-0.5mm]{$\hat x_{t-1}, u_{t-1}$};

% Closed-loop action arrow (controller -> env), routed under the figure
\draw[->, line width=0.8pt, color=violet!70!black]
    (ctl.south) -- ++(0,-23mm) coordinate (c)
    -| node[label, near start, below, fill=white]
        {$u_t$ (muscle excitation in $[0,1]^{34}$)} (env.south);

% Eval arrow (env -> eval box; dashed; oracle, never seen by the loop)
\draw[->, dashed, gray] (env.south) -- (eval.north)
   node[label, midway, fill=white]{oracle};

% Background dashed box highlighting "this is the closed loop"
\begin{scope}[on background layer]
\node[draw=gray!50, dashed, rounded corners,
      fit=(env)(obs)(est)(ctl)(fwd),
      inner sep=4mm,
      label={[font=\scriptsize\itshape]above:closed loop}] {};
\end{scope}
\end{tikzpicture}
\caption{Closed-loop pipeline. Each step the env's true state $x_t$ is
filtered through observation wrappers that add per-field Gaussian noise
and a fixed delay, producing $y_t$. The Kalman estimator combines $y_t$
with a one-step prediction $x_{\mathrm{pred}}$ produced by the residual-MLP
forward model $f_\theta(\hat x_{t-1}, u_{t-1})$, weighted by a scalar gain
$K$. The controller consumes only the estimator output $\hat x_t$ and emits
the next muscle excitation $u_t$. The true state is exposed only to the
offline evaluator (dashed red box); the closed loop never accesses it.}
\label{fig:system}
\end{figure*}

\subsection{Forward Model}
A residual MLP $f_\theta(x_t, u_t)$ predicts the state delta
$\Delta x_t {=} x_{t+1}-x_t$ from a flat 83-dim state and 34-dim excitation
input. Architecture: two hidden layers of 256 units with ReLU and
LayerNorm. The model is optimised with Adam \cite{kingma2015adam}.
For the Phase~0 baseline, $f_\theta$ is trained on the standard
single-step MSE loss
\begin{equation}
\mathcal{L}_1(\theta) = \mathbb{E}_{(x_t,u_t,x_{t+1})}\big[\|f_\theta(x_t,u_t)
- \Delta x_t\|^2\big].
\end{equation}

\noindent\textbf{Multi-step rollout supervision.} For Phase~B we also train
with a $K$-step rollout loss. For each contiguous trajectory chunk
$(x_t,u_t,\dots,x_{t+K})$ inside a single source episode, the model is
unrolled free-running:
$\hat x_{t+k} = \hat x_{t+k-1} + f_\theta(\hat x_{t+k-1}, u_{t+k-1})$,
$\hat x_t = x_t$, and the loss averages MSE across the $K$ prediction steps:
\begin{equation}
\mathcal{L}_K(\theta) = \mathbb{E}\Big[\tfrac{1}{K}\sum_{k=1}^K\|\hat x_{t+k}
- x_{t+k}\|^2\Big].
\end{equation}
Multi-step rollout supervision has been used to stabilise long-horizon
predictions in latent-imagination world models~\cite{hafner2020dreamer}
and in model-based policy optimisation~\cite{janner2019mbpo}, where the
relevant horizon is the planner's rollout depth rather than the
estimator's delay; we re-use the technique here to make $f_\theta$
robust across the Kalman buffer.

\noindent We train the same architecture with $K\!\in\!\{1,4,8\}$ on the
same expanded dataset (51\,k transitions across 106 episodes; see
Sec.\,\ref{sec:results-3.1}).

\subsection{Fixed-Lag Kalman Estimator}
Given observation $y_t$ that may be a delayed-and-noisy view of
$x_{t-d}$, we maintain a length-$(d{+}1)$ buffer of past estimates and
actions. At each step we form an inn ovation against the buffer's leftmost
entry (representing $\hat x_{t-d}$), apply gain $K$ element-wise, and roll
the corrected estimate forward through the cached actions to obtain the
present estimate. A scalar $K\!\in\![0,1]$ is shared across all 83 fields.
The fixed-lag handling follows the standard buffered Rauch--Tung--Striebel
construction \cite{rauch1965,anderson1979optimal}.

\subsection{Learned Gain (Stage~A)}
A small MLP $g_\phi:\mathbb{R}^{8}\!\to\![0,1]$ maps a condition feature
vector --- 3 controller one-hot bits, 4 noise sigma fields, and one
normalised delay --- to a scalar gain $K_\phi$ via sigmoid. The model is
trained on the per-cell oracle $K^{\star}$ produced by a fixed-K grid
sweep on the same noise/delay grid, minimising MSE against $K^{\star}$ with
$N{=}72$ examples (5 K $\times$ 24 conditions $\to$ argmin).
Section~\ref{sec:results-3.6} (R6) replaces the open-loop oracle with a
closed-loop one with no other changes to $g_\phi$.

\subsection{Controllers}
Three controllers are evaluated as closed-loop wrappers around the
estimator: (i)~a heuristic muscle controller
$u {=} \sigma(b + g\,W\!\cdot\!{-}r)$ where $W{\in}\mathbb{R}^{34\times 3}$
is a fixed random projection and $r$ is the estimated reach error; (ii)~a
joint-space PD controller with one-shot damped-pseudoinverse inverse
kinematics
$q^{\star} {=} \mathrm{IK}(p^{\star})$ giving $u_{\text{joint}}{=}
K_p(q^{\star}\!-\!\hat q)\!-\!K_d\hat{\dot q}$ and a moment-arm muscle
mapping
$u_{\text{muscle}} {=} \mathrm{clip}(\alpha\,\mathrm{ReLU}
(M\,u_{\text{joint}}), 0, 1)$;
and (iii)~a small behaviour-cloned MLP trained on a 30-episode scripted
IK demonstration dataset in the spirit of~\cite{pomerleau1988alvinn};
we do not iterate the demonstrator with DAgger~\cite{ross2011dagger} in
this work and discuss the resulting open-loop collapse in
Sec.\,\ref{sec:results-3.4}. Tuned gains:
$K_p{=}30$, $K_d{=}3$, $\alpha{=}5$.

\subsection{Evaluation Pipeline}
\emph{Open-loop} evaluation replays a recorded episode and applies
observation wrappers to synthesise $y_t$ for an arbitrary $(\sigma, d)$
combination; the estimator is then run on the synthesised stream and
per-field state error is aggregated. \emph{Closed-loop} evaluation drives
the actual env: $u_t$ comes from the controller, which sees the estimator
output (never the true state); 10 episodes per cell, fixed seed per episode.
The same evaluation grid (3 controllers $\times$ 6 noise levels $\times$ 4
delays $=72$ conditions for open-loop, focused $3{\times}2{\times}10{=}60$
for closed-loop) is reused across phases.

% ============================================================
\section{Results}
\label{sec:results}
% ============================================================

\subsection{Forward model improvement cycle}
\label{sec:results-3.1}

The Phase~0 baseline ($K{=}1$ single-step loss) trained on the original
$\sim 3.6$\,k transitions from 6 baseline episodes already produced a
forward model with low single-step error but unbounded long-horizon
rollouts ($h{=}50$ MSE $\sim 10.6$). After collecting 100 additional
\texttt{random} and \texttt{lowamp} episodes (total $\sim 51$\,k
transitions across 106 episodes) and retraining, $h{=}50$ MSE dropped to
0.052 --- a $200\times$ improvement that we use as the single-step
baseline throughout this paper.

\subsection{Where blending matters: open-loop oracle (R1)}
\label{sec:results-3.2}

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{F2_stress_oracle_K.pdf}
\caption{Open-loop oracle Kalman gain $K^{\star}$ as a function of
observation delay and noise level, averaged over 3 controllers and 4
delays $\times$ 6 noise levels $\times$ 3 controllers $\times$ 5 K
sweep $= 360$ cells. Three forward-model variants: single-step, $K{=}4$
multi-step, and $K{=}8$ multi-step. Under single-step supervision the
$K{<}1$ regime is confined to zero delay; multi-step supervision widens
it to delay $\geq 6$\,steps in high-noise cells.}
\label{fig:F2_oracle}
\end{figure*}

Fig.\,\ref{fig:F2_oracle} (left panel) shows the oracle $K^{\star}$ under
the single-step-supervised forward model. At zero delay $K^{\star}$
decreases monotonically from $1.0$ (no noise) to $0.25$ (extreme noise),
recovering the textbook intuition that high observation noise warrants
lower trust in the sensor. At delay~$\geq 18$\,steps, however, $K^{\star}$
is uniformly $1.0$: the rollout error compounds faster than observation
noise grows, so even noisy observations beat the propagated forward-model
estimate. Of 72 conditions only 18 (25\%) have $K^{\star}{<}1$.

\subsection{Closed-loop differentiation, single-step model (R3)}
\label{sec:results-3.3}

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{F4_phase2d_delta_heatmap.pdf}
\caption{Joint-PD closed-loop reaching: $\Delta$ between the learned
condition-level predictor and the $K{=}1$ baseline, per (noise, delay)
cell. The $-8.6$\,cm signal at (none, $d{=}18$) is the only cell where
the learned predictor clearly beats $K{=}1$.}
\label{fig:F4_phase2d}
\end{figure}

We evaluate three estimators ($K{=}0$, $K{=}1$, Stage~A learned) under the
joint-PD controller on $3$ noise $\times$ 2 delay $\times$ 10 episodes.
Fig.\,\ref{fig:F4_phase2d} shows the $\min$-tip differences between the
learned predictor and the $K{=}1$ baseline. The learned predictor beats
$K{=}1$ by 8.6\,cm at (none, $d{=}18$) --- exactly the regime where the
open-loop oracle says blending is least useful, but where in closed loop
the controller's reliance on the buffered prediction makes a tighter
estimate pay off. Other cells differ by less than $2$\,cm.

The trajectory plot in Fig.\,\ref{fig:F5_traj} shows that at this cell
$K{=}0$ diverges (the prediction-only rollout is too inaccurate over
$d{=}18$\,steps), $K{=}1$ approaches but oscillates around the buffered
target, and the learned estimator reaches further.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{F5_phase2d_trajectory.pdf}
\caption{Representative closed-loop trajectories at (noise=none,
$d{=}18$): mean $\pm$ std of $\min$-tip error across 10 episodes for
each estimator.}
\label{fig:F5_traj}
\end{figure}

\subsection{Behaviour-cloned controllers wash out the estimator signal (R4)}
\label{sec:results-3.4}

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{F6_controller_tradeoff.pdf}
\caption{Trade-off between reaching success and estimator differentiation
across five controller variants. The joint-PD+IK controller sits in
the high-differentiation / low-reaching regime; BC variants improve
reaching but lose the estimator signal.}
\label{fig:F6_tradeoff}
\end{figure}

To check whether the 8.6\,cm signal generalises, we replace the joint-PD
controller with a small behaviour-cloned MLP trained on a 30-episode
scripted IK demonstration set. Reaching improves substantially
($\mathrm{success}{<}0.1\,\mathrm{m}$ rate increases from $\sim 12\%$ to
$\sim 30\%$ in the easiest cells), but the gap between the learned
predictor and $K{=}1$ collapses to $<3$\,cm in all cells
(Fig.\,\ref{fig:F6_tradeoff}). Ablating \texttt{target\_pos} alone, or
both \texttt{target\_pos} and \texttt{reach\_err}, from the BC input does
not restore differentiation: the BC policy's open-loop average reach
trajectory does not actually consume per-step state estimates strongly
enough to surface estimator differences. We treat this as a controller-
side result and return to it in the discussion.

\subsection{Long-horizon supervision flips the closed-loop optimum (R5)}
\label{sec:results-3.5}

\begin{figure*}[t]
\centering
\includegraphics[width=0.85\textwidth]{F7_paradigm_shift.pdf}
\caption{Closed-loop reaching min-tip error under the joint-PD controller.
Four forward-model / oracle variants. Top: single-step-supervised forward
model. Second: $K{=}4$ multi-step. Third: $K{=}8$ multi-step. Bottom:
$K{=}8$ forward model with Stage~A retrained on the closed-loop oracle
(Phase~C). The orange $K{=}0$ bar drops monotonically as multi-step
supervision strengthens; in panel~4 the green learned bar matches the
$K{=}0$ baseline at delay-18 cells.}
\label{fig:F7_paradigm}
\end{figure*}

Retraining the forward model with $K$-step rollout supervision
$\mathcal{L}_K$ drops $h{=}50$ tip prediction error to 0.064\,m
($K{=}4$, $-54\%$ vs.\ baseline) and 0.048\,m ($K{=}8$, $-65\%$). The
open-loop oracle then expands the $K^{\star}{<}1$ regime from 18 to 26
($K{=}4$) and 27 ($K{=}8$) of 72 cells; blending becomes optimal at delay
$\geq 6$\,steps in high-noise cells
(Fig.\,\ref{fig:F2_oracle}, middle and right panels).

In \emph{closed loop} the picture is opposite: under both $K{=}4$ and
$K{=}8$, $K{=}0$ becomes the best estimator across all 6 cells of the
focused grid, with $\min$-tip dropping to $\sim 0.53$\,m ($K{=}4$) and
$\sim 0.49$\,m ($K{=}8$) versus $K{=}1$ at 0.66--0.77\,m. Notably $K{=}1$
\emph{worsens} from 0.60 to 0.70\,m as multi-step supervision strengthens
at delay-18 cells: a stronger forward model makes the observation
correction it is being asked to apply less compatible with the planned
trajectory.

The condition-level learned predictor, however, keeps outputting
$K\!\approx\!0.36$--$0.99$ (Fig.\,\ref{fig:F7_paradigm}, panels 2--3)
because the open-loop oracle it was trained on never picks $K{=}0$ --- at
delay $\geq 18$ even with $K{=}8$ supervision, the open-loop oracle still
selects $K^{\star}{=}1$ or close to it (the open-loop estimation error is
minimised by trusting the observation when the buffer rollout is long).
The optimum gap thus widens, not closes, with supervision strength.

\subsection{Closed-loop-aware oracle resolves the gap (R6)}
\label{sec:results-3.6}

We address the gap by changing only the oracle definition. Under the
$K{=}8$ forward model we sweep $K\!\in\!\{0,0.25,0.5,0.75,1.0\}$ in
closed loop on the same 6 cells and pick $K^{\star}_{\text{cl}}$ per cell
by minimising closed-loop $\min$-tip error. The result is unanimous:
$K^{\star}_{\text{cl}}{=}0$ in every cell. We retrain the same MLP
$g_\phi$ on the 18 $K^{\star}_{\text{cl}}{=}0$ labels (3 controllers
$\times$ 6 cells); the predictor now outputs $K\!\approx\!0.018$--$0.025$.

Plugged back into closed loop (Fig.\,\ref{fig:F7_paradigm}, panel~4) the
new predictor matches the $K{=}0$ baseline within $1.5$\,cm at every
delay-18 cell (and slightly beats $K{=}0$ at (xhigh, $d{=}18$)). At
delay-0 cells a $\sim 17$\,cm gap remains, because the closed-loop
K-curve at $d{=}0$ jumps sharply from 0.49\,m at $K{=}0$ to 0.77\,m at
$K{=}0.25$, and the sigmoid asymptote (0.025) loses $\sim 0.17$\,m. Across
all 6 cells the closed-loop-aware predictor closes 40--60\% of the
single-step paradigm gap; mean improvement vs.\ the open-loop-oracle
predictor is $-16$\,cm. No architectural changes were made.

% ============================================================
\section{Discussion}
\label{sec:discussion}
% ============================================================

\noindent\textbf{Forward-model accuracy reshapes the closed-loop optimum.}
Phase~B/B' shows that the $K^{\star}$ that minimises open-loop estimation
error is not the same as the $K^{\star}$ that minimises closed-loop task
error. As the forward model becomes accurate enough to roll out over the
controller's planning horizon, observation correction starts to actively
hurt rather than help. The condition-level predictor trained on the
open-loop oracle is then mismatched by 0.5--0.7 in $K$ from the closed-loop
truth. We interpret this as a structural finding rather than an artifact:
the same arm dynamics and observation noise produce different optimal
estimators depending on whether the readout is the residual of a one-step
update or the cumulative effect of feedback control over hundreds of steps.

\smallskip

\noindent\textbf{Oracle redefine is a cheap fix.} R6 shows that this
mismatch is mostly resolvable by training the predictor on a closed-loop
K-sweep rather than an open-loop one --- no change to the predictor MLP
or its capacity. The remaining $\delta\!\sim\!17$\,cm gap at zero delay
is parameterisation-bound: a sigmoid cannot output $K{=}0$ exactly,
and the closed-loop K-curve at $d{=}0$ is sharp around 0. A Hardtanh or
output-clipped variant should close this further; we leave that to
future work.

\smallskip

\noindent\textbf{Behaviour cloning is not a good benchmark for state
estimation.} R4 shows that, at modest demonstration scale, a behaviour-
cloned policy improves reaching but consumes state estimates so weakly
that estimator differences disappear from task metrics. Future closed-loop
estimator benchmarks should use controllers that are demonstrably
state-coupled (e.g., the joint-PD+IK family used here); a BC policy that
generalises poorly per target is effectively open-loop in the state
channel even though it nominally consumes a state vector.

\smallskip

\noindent\textbf{Limits and open questions.} (a)~The closed-loop oracle
sweep we use is expensive ($K_\mathrm{grid}\times$ cells $\times$ episodes
rollouts); a differentiable surrogate would scale this far better.
(b)~Our learned predictor is condition-level (no per-step state coupling
beyond the sigma vector and delay scalar); a state-aware Stage~B variant
we explored briefly did not beat the condition-level baseline because of
labelling noise, but a closed-loop differentiable label might. (c)~All
results are on a single MyoSuite reach task; transfer to grasp/place
tasks is open. (d)~The 17\,cm residual at $d{=}0$ is structural and could
be closed with a predictor parameterisation that supports $K{=}0$
exactly.

% ============================================================
\section{Conclusion}
\label{sec:conclusion}
% ============================================================

We have shown, on a muscle-driven reaching task, that the closed-loop
optimal Kalman gain depends on both the forward model's accuracy and the
oracle used to train any learned gain predictor. Single-step supervision
of the forward model creates a small but real regime where a learned
condition-level gain beats observation-only (R3, $-8.6$\,cm). Multi-step
supervision flips the regime: $K{=}0$ becomes globally optimal in closed
loop (R5). Redefining the predictor's oracle in terms of closed-loop task
error rather than open-loop estimation accuracy aligns the predictor with
this new regime and recovers most of the gap (R6) with no architectural
changes. We argue that learned-Kalman-gain papers should specify the
oracle target explicitly and that closed-loop benchmarks should be a
standard part of evaluating adaptive state estimators.

% ============================================================
\section*{Acknowledgement}
% ============================================================
\noindent We thank [redacted] for [redacted].

\bibliographystyle{IEEEtran}
\bibliography{refs}

\end{document}
